# 🏪 连锁店多分店系统 - 完整功能说明

## ✅ 已实现的功能

### 1. **用户角色与权限系统**

#### 四种角色：
- **👑 超级管理员 (super_admin)**
  - 查看所有分店数据
  - 管理分店信息（添加/编辑/删除）
  - 访问所有功能模块
  
- **🏢 店长 (store_manager)**
  - 只能查看和管理自己分店的数据
  - 访问大部分管理功能
  - 无法访问分店管理模块
  
- **💰 收银员 (cashier)**
  - 直接进入POS收银界面
  - 简化菜单（只有POS相关功能）
  
- **👨‍🍳 厨师 (chef)**
  - 直接进入厨房显示界面
  - 简化菜单（只有厨房相关功能）

---

### 2. **分店管理系统** (`/stores`)

#### 功能特性：
- ✅ 添加新分店
- ✅ 编辑分店信息
- ✅ 删除分店
- ✅ 分店状态管理（营业中/已关闭）
- ✅ 分店代码管理（如 MN001, LE002）
- ✅ 配置分店参数：
  - 地址、电话
  - 货币单位（C$/$）
  - 税率
  - 营业时间
  - 店长信息

#### 统计卡片：
- 总分店数
- 营业中分店
- 已关闭分店

---

### 3. **智能登录系统**

#### 登录流程：
1. **选择角色** - 下拉选择4种角色之一
2. **选择分店** - 非超级管理员需要选择分店
3. **自动跳转** - 根据角色跳转到不同页面：
   - 收银员 → POS收银
   - 厨师 → 厨房显示
   - 其他 → 仪表板

#### 用户体验：
- 显示当前用户信息（右上角）
- 显示当前分店名称（顶部导航栏）
- 一键退出登录

---

### 4. **基于角色的菜单过滤**

#### 菜单可见性：
```
超级管理员: 全部9个菜单
店长:       8个菜单（无分店管理）
收银员:     1个菜单（仅POS）
厨师:       1个菜单（仅厨房）
```

#### 菜单列表：
- 📊 仪表盘 - 管理员、店长
- 💰 POS收银 - 所有人
- 👨‍🍳 厨房显示 - 管理员、店长、厨师
- 📦 库存管理 - 管理员、店长
- 👥 员工管理 - 管理员、店长
- 🤝 客户管理 - 管理员、店长
- 🏢 店长管理 - 管理员、店长
- 🏪 分店管理 - 仅超级管理员 ⭐
- 📈 报表中心 - 管理员、店长

---

### 5. **智能导航栏**

#### 显示策略：
- **POS/厨房页面**: 完全隐藏侧边栏（全屏工作区）
- **管理页面**: 可折叠侧边栏
  - 展开：16rem宽，图标+文字
  - 折叠：4rem宽，只显示图标
- **顶部导航栏**: 
  - 显示系统名称
  - 显示当前分店（如果有）
  - 显示用户信息和角色
  - 折叠/展开按钮

---

## 📋 使用示例

### 场景1：超级管理员日常工作

```
1. 登录
   - 角色：超级管理员
   - 无需选择分店

2. 查看总览
   - 进入仪表板
   - 可以看到所有分店汇总数据

3. 管理分店
   - 点击"分店管理"
   - 添加新分店：格拉纳达分店
   - 设置分店代码：GR003
   - 配置营业时间和税率

4. 切换分店查看
   - （未来功能：顶部分店选择器）
   - 查看不同分店的详细数据

5. 生成报表
   - 进入报表中心
   - 选择日期范围
   - 查看所有分店或单个分店的财务报表
```

---

### 场景2：店长管理单店

```
1. 登录
   - 角色：店长
   - 选择分店：马那瓜总店

2. 日常工作
   - 进入仪表板
   - 只看自己分店的数据
   - 监控销售情况

3. 员工管理
   - 添加新员工
   - 排班管理
   - 考勤审核

4. 薪资结算
   - 为员工结算工资
   - 处理借款
   - 生成工资单

5. 查看报表
   - 生成分店日报/月报
   - 分析经营数据
```

---

### 场景3：收银员工作

```
1. 登录
   - 角色：收银员
   - 选择分店：马那瓜总店
   - 自动跳转到POS界面

2. POS收银
   - 全屏工作区（无侧边栏）
   - 点餐、结账
   - 打印小票

3. 退出
   - 点击右上角退出登录
```

---

### 场景4：厨师工作

```
1. 登录
   - 角色：厨师
   - 选择分店：马那瓜总店
   - 自动跳转到厨房界面

2. 厨房显示
   - 全屏显示待制作订单
   - 大字体，易读
   - 标记完成

3. 退出
   - 点击返回按钮
```

---

## 🔧 技术实现

### 1. 认证上下文 (AuthContext)
```typescript
interface User {
  id: string;
  username: string;
  name: string;
  role: UserRole;
  storeId?: string;
  storeName?: string;
}

// 提供：
- login(user) - 登录
- logout() - 退出
- switchStore(storeId, storeName) - 切换分店
- isAuthenticated - 是否已登录
```

### 2. 数据存储
```javascript
localStorage:
- current_user: 当前登录用户
- stores: 分店列表
- employees: 员工（含storeId）
- orders: 订单（含storeId）
- ...其他数据
```

### 3. 数据过滤
```typescript
// 自动根据用户角色和分店过滤数据
const getData = () => {
  const allData = loadData();
  
  if (user.role === 'super_admin') {
    return allData; // 返回所有数据
  } else {
    return allData.filter(item => item.storeId === user.storeId);
  }
};
```

---

## 🎯 下一步扩展建议

### 1. 分店选择器（超级管理员用）
在顶部导航栏添加分店下拉选择框，超级管理员可以快速切换查看不同分店的数据。

### 2. 跨分店数据汇总
在仪表板添加：
- 所有分店销售排名
- 分店对比图表
- 总体经营趋势

### 3. 数据隔离强化
确保所有API调用和数据查询都自动加上storeId过滤，防止数据泄露。

### 4. 分店间调拨
添加库存调拨功能：
- 分店A → 分店B
- 调拨记录
- 库存同步

### 5. 统一采购
超级管理员可以：
- 集中采购
- 分配到各分店
- 跟踪配送状态

---

## 📊 系统架构图

```
┌─────────────────────────────────────┐
│         前端 React App              │
├─────────────────────────────────────┤
│  AuthContext (认证状态管理)          │
│  ├─ user: 当前用户                   │
│  ├─ role: 用户角色                   │
│  └─ storeId: 当前分店                │
├─────────────────────────────────────┤
│  MainLayout (智能布局)               │
│  ├─ 角色菜单过滤                     │
│  ├─ 导航栏显示/隐藏                  │
│  └─ 分店信息显示                     │
├─────────────────────────────────────┤
│  功能模块                            │
│  ├─ Stores (分店管理) ⭐             │
│  ├─ Dashboard (仪表板)               │
│  ├─ POS (收银)                       │
│  ├─ Kitchen (厨房)                   │
│  ├─ Inventory (库存)                 │
│  ├─ Employees (员工)                 │
│  ├─ Customers (客户)                 │
│  ├─ Manager (店长管理)               │
│  └─ Reports (报表)                   │
└─────────────────────────────────────┘
         ↓ localStorage
┌─────────────────────────────────────┐
│      数据层（按分店隔离）             │
├─────────────────────────────────────┤
│  stores: 分店列表                    │
│  employees: 员工 (含storeId)         │
│  orders: 订单 (含storeId)            │
│  inventory: 库存 (含storeId)         │
│  ...所有业务数据                     │
└─────────────────────────────────────┘
```

---

## 💡 关键特性总结

✅ **完整的角色权限系统** - 4种角色，精细控制
✅ **分店管理模块** - 添加/编辑/删除分店
✅ **智能登录流程** - 根据角色自动跳转
✅ **基于角色的菜单** - 不同角色看到不同菜单
✅ **智能导航栏** - POS/厨房全屏，管理后台可折叠
✅ **数据隔离** - 店长只能看自己分店
✅ **用户信息显示** - 顶部显示用户名、角色、分店
✅ **一键退出** - 清除登录状态

---

## 🚀 如何使用

1. **刷新浏览器** - 加载最新代码
2. **访问登录页** - http://localhost:3000/login
3. **选择角色测试**：
   - 选"超级管理员" → 可以看到"分店管理"菜单
   - 选"店长" → 看不到"分店管理"，其他都有
   - 选"收银员" → 直接进入POS
   - 选"厨师" → 直接进入厨房
4. **点击"分店管理"** - 管理所有分店信息
5. **查看顶部信息** - 显示当前用户和分店

---

**系统已经完全支持连锁店多分店架构！** 🎉
