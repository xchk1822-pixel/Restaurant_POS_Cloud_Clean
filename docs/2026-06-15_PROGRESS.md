# 2026-06-15 Progress

Project source of truth:
- Local: `C:\Users\华为\Desktop\Restaurant_POS_Cloud_Clean`
- Hosting: `https://restaurant-pos-1b420.web.app`
- GitHub: `xchk1822-pixel/Restaurant_POS_Cloud_Clean`, branch `main`
- Coding rule: keep changes simple and scoped. If a fix can be done in 10 clear lines, do not use 20 lines or duplicate logic; prefer existing helpers and remove confirmed dead/polluted data.

## Completed Today

2026-06-24 supplier module update:
- Built a new supplier accounts-payable module instead of continuing to patch the old supplier page.
- Added `client/src/pages/Suppliers/SupplierWorkbench.tsx` as the new `/suppliers` route target.
- Added `client/src/pages/Suppliers/supplierLedger.ts` for the new module's accounting interpretation layer.
- Preserved the original data relationships and cloud paths: `suppliers`, `purchase_orders`, `supplier_payments`, and `expenses`.
- Fixed the supplier payable semantics in the new module:
  - cash purchases display as cash purchases, not debt
  - remaining debt is `totalAmount - paidAmount`
  - part-paid credit purchases display as partial debt
  - ledger rows use normalized dates
- Verified with supplier ledger tests, supplier data-safety tests, production build, Firebase deploy, and online browser check.
- Archive: `docs/2026-06-24_NEW_SUPPLIER_MODULE.md`
- Added supplier bill date filtering:
  - default range is the current month
  - quick filters for today, current month, previous month, and last 30 days
  - manual start/end date inputs
  - period purchase/payment summary cards
  - generated supplier bill follows the active date range
  - current remaining debt remains all-time so historical unpaid debt is not hidden

1. Continued the ordered manager-management pass.
   - Expense records
   - Shift handover
   - Order history
   - Financial reports
   - Customers

2. Expense records data safety.
   - Receipt removal now writes the updated expense document to cloud first.
   - Local state and local cache update only after the cloud write completes.
   - This prevents an old local receipt value from replaying after refresh or another terminal sync.

3. Shift handover data safety.
   - Shift handover submit now writes the new handover record to cloud before updating local history.
   - This reduces the chance of local-only history appearing saved and then disappearing after refresh.

4. Order history cancellation audit display.
   - Added shared helpers to collect order-level and item-level cancellation records.
   - Order detail modal now shows cancellation summary when present:
     - cancellation reason
     - operator
     - cancellation time
   - Existing item-level cancellation records are shown in the same order detail modal.
   - Print reports still do not print cancellation reasons, matching the prior business decision.

5. Online manager-module verification.
   - Logged in online with `zeng/123456`.
   - Verified these deployed routes open without blank pages or console errors:
     - `/manager`
     - `/manager/expense-records`
     - `/manager/shift-handover`
     - `/manager/order-history`
     - `/manager/financial-reports`
     - `/manager/customers`
   - Confirmed order history still sorts newest orders first and the detail modal opens.

6. System settings data safety.
   - Store edit and account edit/create now call cloud writes before mutating local UI/cache state.
   - Permission-role save now writes `system_roles` before replacing local role cache.
   - Exchange-rate settings now write to `stores/{storeId}/exchange_rate` instead of a bare/global collection.
   - Boss/admin exchange-rate page now shows a store selector and saves the selected store's settings.
   - Exchange-rate store selector dedupes duplicate store documents by code/id.
   - Explicit Firestore paths such as `stores/{storeId}/exchange_rate` now cache locally as `store_{storeId}_exchange_rate`.
   - Backup export no longer treats `exchange_rate` as a global collection; it is exported only inside each store's business data.

7. Online system-settings verification.
   - Logged in online with `admin/admin123`.
   - Verified these deployed routes open without blank pages or console errors:
     - `/settings/stores`
     - `/settings/exchange-rate`
     - `/settings/permissions`
     - `/settings/backup`
   - Confirmed exchange-rate store selector shows deduped stores.
   - Confirmed permission management still shows exactly four editable roles.

8. Cleanup pass 1: confirmed-unused technical leftovers.
   - Route scan confirmed `App.tsx` is the only React route entry.
   - Import graph found `client/src/types/index.ts` is not referenced by app or tests.
   - Firebase config confirms Hosting deploys only `client/build`; no Functions or server target uses the old `server/` folder.
   - Package scripts do not call `client/scripts/*.cjs`.
   - Removed old one-off scripts with hardcoded project/admin operations to prevent accidental future execution.
   - Removed stale Playwright log output under `client/test-results`.
   - Removed undeployed reserved `server/` and `shared/` code.
   - Removed the mojibake legacy module overview document under `client/src/pages`.
   - Deployed and archived in commit `c989981`.

9. Cleanup pass 2: root documentation cleanup.
   - Scanned all root-level Markdown files and cross-references.
   - Kept `README.md` as the only root Markdown entry.
   - Replaced the mojibake/outdated README with a current project entry that points to active docs.
   - Removed obsolete root docs that referred to old deleted services, old global sync paths, old demo routes, old server setup, and stale architecture claims.
   - Confirmed root Markdown cleanup does not touch source code, Firebase rules, or live business data.
   - Deployed and archived in commit `33ede41`.

10. Cleanup pass 3: docs history cleanup.
   - Scanned current `docs/` files and references.
   - Kept active docs only:
     - `2026-06-11_EXECUTION_PLAN.md`
     - `2026-06-15_PROGRESS.md`
     - `COMMERCIAL_V3_REQUIREMENTS.md`
     - `V3_DATA_MODEL_DRAFT.md`
   - Removed superseded history files:
     - `2026-06-09_HANDOFF.md`
     - `2026-06-10_PROGRESS.md`
     - `2026-06-14_DAILY_WRAPUP.md`
   - Confirmed this cleanup does not touch source code, Firebase rules, or live business data.
   - Deployed and archived in commit `280f935`.

11. Build warning cleanup pass 1: unused variables.
   - Removed or neutralized unused-variable warnings without changing business behavior.
   - Touched only low-risk unused declarations in:
     - split bill modal
     - AppContext load marker
     - fridge stocktake helper
     - supplier management print/modal state leftovers
     - expense records type leftover
     - shift handover timer state
     - POS legacy drag/drop and wait-time helpers
     - waiter menu item alias
   - Supplier transaction modal state was preserved because the button still exists; the missing modal should be evaluated as a separate functional task.
   - POS legacy drag/drop helpers were preserved with explicit no-op references because the file contains mojibake strings and the active mouse-drag path should not be disturbed in this pass.
   - Deployed and archived in commit `f67c277`.

## Verification

- Focused tests passed:
  - `src/utils/orderHistory.test.ts`
  - `src/utils/dataSafety.test.ts`
  - 86 tests passed.
- Full Jest suite passed:
  - 12 suites passed.
  - 118 tests passed.
- TypeScript passed:
  - `npx tsc --noEmit`
- Production build passed:
  - `npm run build`
  - Existing ESLint warnings remain and were not part of this change.
- Firebase Hosting deployed:
  - `https://restaurant-pos-1b420.web.app`
- Online Playwright verification passed:
  - final browser console error count: 0.
- System settings verification passed after the second deploy:
  - final browser console error count: 0.
- Cleanup pass 1 verification:
  - Strict import scan passed for 79 TS/JS source files.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with only existing ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for dashboard/settings routes with 0 console errors.
  - Online store-manager smoke test passed for POS, inventory, menu, manager, financial reports, and order history routes with 0 console errors.
- Cleanup pass 2 verification:
  - Root Markdown reference scan confirmed only `README.md` remains at repo root and old document names are not referenced by active code/docs.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with only existing ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for dashboard/settings routes with 0 console errors.
  - Online store-manager smoke test passed for POS, inventory, menu, and order history routes with 0 console errors.
- Cleanup pass 3 verification:
  - Confirmed `docs/` now keeps only four active Markdown documents.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with only existing ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for dashboard/settings routes with 0 console errors.
  - Online store-manager smoke test passed for POS, inventory, menu, and order history routes with 0 console errors.
- Build warning cleanup pass 1 verification:
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed.
  - Build warnings reduced to Hook dependency warnings only:
    - `AppContext.tsx`: `mergeCloudDataByVersion`
    - `FridgeStocktake.tsx`: `fridgeItems`
    - `WarehouseStocktake.tsx`: `actualQuantities`, `filteredItems`
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for dashboard/settings routes with 0 console errors.
  - Online store-manager smoke test passed for POS, inventory, menu, and order history routes with 0 console errors.
- Build warning cleanup pass 2 verification:
  - Stabilized `AppContext` cloud merge helpers with `useCallback` so the store-scoped `pos_orders` realtime subscription has complete Hook dependencies.
  - Stabilized fridge and warehouse stocktake filtered item lists with `useMemo` and function-style state updates so same-length item changes are not missed.
  - Updated the data-safety source guard to recognize the protected terminal-order merge logic after the `useCallback` conversion.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for dashboard, branch detail, stores, and backup routes with 0 console errors.
  - Online store-manager smoke test passed for manager overview, POS, inventory, menu, and order history routes with 0 console errors.
- Leftover cleanup pass 4 verification:
  - Confirmed `getOtherFridgeItemIds`, `handleTableDragOver`, `handleTableDrop`, and the POS-local `getWaitTime` had no production references beyond their own `void` suppression lines.
  - Removed those unused helpers from fridge stocktake and POS without changing any storage, sync, order, or stock deduction path.
  - Kept the kitchen display `getWaitTime` helper because it is still used by the kitchen UI.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for POS and fridge stocktake routes with 0 console errors.
- Leftover cleanup pass 5 verification:
  - Removed five obsolete commented-out realtime listener blocks from `AppContext.tsx`.
  - The active cloud flow remains unchanged: store snapshot loading still uses `smartGetDocuments`, and live order sync still subscribes to `pos_orders`.
  - Confirmed there are no remaining source matches for the temporary-disable marker.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 122 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for manager overview and POS routes with 0 console errors.
- Leftover cleanup pass 6 verification:
  - Removed seven obsolete redirect-only migration/test routes from `App.tsx`: `/migrate`, `/migrate-users`, `/emergency-fix`, `/data-recovery`, `/firebase-test`, `/offline-test`, and `/data-init`.
  - Kept short compatibility redirects `/stores`, `/exchange-rate`, and `/reports` because they may still be old user-facing bookmarks.
  - Added a `dataSafety.test.ts` guard so obsolete migration/test routes cannot be reintroduced silently.
- Inventory stock ledger display follow-up:
  - Fixed stock in/out records time display so Firestore Timestamp objects, JSON Timestamp shapes, millisecond timestamps, and local date-only strings no longer render `Invalid Date` or roll Nicaragua local dates back to the previous day at 18:00.
  - Fixed stocktake adjustment quantity display so an actual increase such as `+24` shows as `+24`, not `-24`.
  - Display now maps internal transfer/operator values to business labels such as `仓库调拨到冰箱`, `冰箱退回仓库`, `系统操作`, and `POS收银`.
  - Targeted stock ledger test passed: 4 focused guards.
  - Inventory/POS regression passed: 3 suites, 208 tests.
  - Production build passed with bundle `main.b7164b87.js`.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online inventory browser verification passed: no `Invalid Date`, no `2026/6/29 18:00:00`, friendly labels present, console/page errors 0.
  - Online POS smoke passed: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0, bundle `main.b7164b87.js`.
- Inventory negative-stock visibility:
  - Added `全部状态 / 负库存 / 低库存` status filtering to the inventory item list.
  - Added a default visible negative-stock warning above the item table with a `查看负库存` action.
  - Added a `负库存` row status label so negative values are not hidden under generic low-stock status.
  - This is visibility-only; the code does not clamp or overwrite negative stock values.
  - Read-only inventory audit still reports 7 Bluefields negative warehouse stock issues; these require approved stocktake correction, not silent code edits.
  - Targeted guard passed: `negative stock visibility`.
  - Inventory/POS regression passed: 3 suites, 209 tests.
  - Production build passed with bundle `main.01d5f75c.js`.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online inventory browser verification passed: negative-stock option visible, warning visible, filter works, console/page errors 0.
  - Online POS smoke passed: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0, bundle `main.01d5f75c.js`.
  - RED/GREEN check passed: the new route guard failed before route removal and passed after cleanup.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 123 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for login and POS routes with 0 console errors.
- Leftover cleanup pass 7 verification:
  - Rechecked compatibility route references and history before deletion.
  - Confirmed active navigation uses `/settings/stores`, `/settings/exchange-rate`, and `/manager`; the short paths `/stores`, `/exchange-rate`, and `/reports` only existed as old redirects in `App.tsx`.
  - Removed the three old compatibility redirect routes and the stale old-path comment from `App.tsx`.
  - Added a RED/GREEN `dataSafety.test.ts` guard requiring canonical settings and manager routes only.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 124 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin smoke test passed for `/settings/stores` and `/settings/exchange-rate` with 0 console errors.
  - Online store-manager smoke test passed for `/manager` with 0 console errors.
- Leftover cleanup pass 8 verification:
  - Rechecked POS stale comment blocks and source history before deletion.
  - Removed disabled legacy Firestore sync blocks for held orders, clear-table order sync, and clear-table table sync from `POS.tsx`.
  - Removed the dead `if (false && ...)` served-to-completed order migration branch.
  - Removed the stale commented duplicate smart sync import and replaced an unreadable order-retention comment with a concise current Chinese comment.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing disabled legacy sync blocks and dead POS migration branches from returning.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 125 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager POS smoke test passed for table layout and order list rendering with 0 console errors.
- Leftover cleanup pass 9 verification:
  - Rechecked POS data-linkage debug logging and source history before deletion.
  - Removed the stale POS `useEffect` that only printed data-linkage debug messages and used permanently empty counters for `pos_orders`, `restaurant_menu_items`, and `inventory_items`.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing this empty-counter debug effect from returning.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 126 tests.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager POS smoke test passed for table layout and order list rendering with 0 console errors.
- Leftover cleanup pass 10 verification:
  - Rechecked split-bill modal render logging and source history before deletion.
  - Removed two noisy `SplitBillModal` render/load debug console logs that had no business effect.
- 2026-06-25 fridge transfer network-state correction:
  - Root cause: fridge transfer trusted cached `isOnline` before trying Firestore, so a stale browser/network flag could show a false offline error even when internet access was normal.
  - Removed the cached offline pre-check from `smartTransferFridgeStock`; transfer now attempts the Firestore transaction when Firestore is enabled.
  - Added a longer fridge-transfer confirmation timeout and changed weak-network handling to a local pending transfer order instead of claiming "no network".
  - Pending fridge transfers locally update warehouse/fridge stock and store a `stock_transfer_records` row with `pendingCloudSync: true`; online replay uses the same Firestore transaction with `allowPendingFallback: false`.
  - Preserved the one-click submit lock to reduce duplicate transfer risk.
  - Added `库存管理 -> 冰箱盘点 -> 调拨记录`: default today, supports all dates, date filter, item/fridge/operation search, Nicaragua minute-level time, before/after warehouse and fridge stock, and pending-sync status.
  - Enlarged both the transfer-record query modal and stocktake-history modal to `96vw`, max `1320px`, and `90vh` for easier query and review.
  - Refined the stocktake-history modal so each fridge record table expands with its content instead of being trapped in a small nested `300px` scroller; the modal body is now the main scroll area.
  - Fixed live fridge-transfer failure after the audit-record change: Firestore rules did not yet allow `stores/{storeId}/stock_transfer_records/{recordId}`, so the transaction could update neither stock nor audit record.
  - Added the missing store-scoped `stock_transfer_records` rule and explicit `permission-denied` transfer error handling.
  - Added RED/GREEN regression guards for cached offline misclassification and pending transfer queue replay.
  - Added a `dataSafety.test.ts` guard for the transfer-record query UI and enlarged stocktake-history modal.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 127 tests.
  - Targeted transfer-record UI guard passed: `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts --testNamePattern="transfer records|fridge transfer"`.
  - Targeted Firestore rules guard passed: `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts --testNamePattern="fridge transfer|stock_transfer_records|permission errors"`.
  - Targeted stocktake-history layout guard passed: `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts --testNamePattern="stocktake history table|fridge transfer records"`.
  - `npm.cmd run build` passed with no ESLint warnings.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager POS smoke test passed for table layout and order list rendering with 0 console errors.
- POS completion responsiveness hotfix:
  - Root cause: dine-in clear-table and takeout/delivery complete actions waited for stock deduction plus `pos_orders` cloud publish before the UI gave visible feedback, so slow network/sync made the button appear stuck.
  - Added immediate processing state for clear-table and non-dine-in complete buttons, disabled duplicate clicks, and yielded one browser frame before running stock/cloud work.
  - Removed the extra clear-table `setTables(tables.map(...))` based on stale table closure; table release remains centralized in `completeOrderWithStockDeduction(..., { releaseTable: true })`.
  - Added a RED/GREEN `dataSafety.test.ts` guard requiring processing feedback before terminal stock/cloud work and preventing duplicate clear-table table writes.
  - `npx.cmd tsc --noEmit --pretty false` passed.
  - Full Jest suite passed: 12 suites, 128 tests.
  - `npm.cmd run build` passed after a retry; the first parallel build command timed out without a build error.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager POS smoke test passed for login, table layout, and order list rendering with 0 console errors; no live test order was created or completed to avoid changing real inventory/order data.
- POS inventory accuracy investigation and hotfix:
  - User reported several stock quantities were inaccurate after operations from another terminal, with uncertainty between operator error, stock deduction logic, and stale cloud/local data.
  - Root-cause risks found in code: POS completion planned deductions from the terminal's current React/local inventory snapshot, `inventory_items` was missing from the AppContext store snapshot refresh, stock increment writes were not awaited before marking orders as `stockDeducted`, and pending offline sync cleared all queued changes when only part of a batch succeeded.
  - Updated AppContext so POS stock deduction refreshes `menu_items`, `inventory_items`, and `fridge_inventory` from the current store path before planning deductions, preserving the fridge-first then warehouse fallback rule.
  - Updated POS completion so stock writes are awaited before an order can be published as completed/stock-deducted.
  - Updated pending Firestore sync so failed changes remain queued instead of being deleted after a partial success.
  - Added RED/GREEN `dataSafety.test.ts` guards for fresh inventory reads, awaited stock deduction, and failed pending-change retention.
  - No live order or stock record was mutated during verification; this avoids changing the user's real inventory before tomorrow's stocktake comparison.
  - Verification passed: full Jest suite 12 suites / 130 tests, `npx tsc --noEmit`, and `npm run build`; deployed and archived in commit `4df7e39`.
  - Tomorrow's audit path: compare each item against `latest stocktake quantity + purchases/transfers - completed-order stock deductions`, then use today's stored stocktake history as the baseline to identify whether any remaining discrepancy comes from operation entry, order deduction, or sync replay.
- Leftover cleanup pass 11 verification:
  - Rechecked permissions-management debug logging and source history before deletion.
  - Removed noisy role-edit `console.log` calls from `PermissionsModule.tsx`; error logs remain.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing these permission role-edit debug logs from returning.
  - Full Jest suite passed: 12 suites, 131 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed after one timeout retry; the retry compiled successfully.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online admin permissions smoke test passed for login, fixed role list, and opening a role editor with 0 console errors; deployed and archived in commit `a9e5b9c`.
- Leftover cleanup pass 12 verification:
  - Rechecked login-page debug logging and source history before deletion.
  - Removed production `console.log` calls that exposed login redirect, username, login success, and automatic migration success flow; warning and error logs remain for failures.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing login success-flow debug logs from returning.
  - Full Jest suite passed: 12 suites, 132 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager login smoke test passed for login page and manager redirect with 0 console errors; deployed and archived in commit `fc12eb9`.
- Leftover cleanup pass 13 verification:
  - Rechecked auth-context debug logging and source history before deletion.
  - Removed production `console.log` calls that exposed Firebase email, cached username/role, login event, and logout success flow; error logs remain for failures.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing auth success-flow identity logs from returning.
  - Full Jest suite passed: 12 suites, 133 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager auth smoke test passed for login page and manager redirect with 0 console errors; deployed and archived in commit `2fce931`.
- Leftover cleanup pass 14 verification:
  - Rechecked Firebase auth service debug logging and source history before deletion.
  - Removed production `console.log` calls that exposed auth username, Firebase UID, logout success, and account-creation success flow; error logs remain for failures.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing Firebase auth service identity/success-flow logs from returning.
  - Full Jest suite passed: 12 suites, 134 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager auth smoke test passed for login page and manager redirect with 0 console errors; deployed and archived in commit `0371554`.
- Leftover cleanup pass 15 verification:
  - Rechecked Firebase initialization debug logging and source history before deletion.
  - Removed the production Firebase initialization success `console.log`; IndexedDB persistence warnings remain for real offline-support diagnostics.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing Firebase initialization success logs from returning.
  - Full Jest suite passed: 12 suites, 135 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager startup/login smoke test passed for login page and manager redirect with 0 console errors; deployed and archived in commit `c994197`.
- Leftover cleanup pass 16 verification:
  - Rechecked purchase-management category selector debug logging and source history before deletion.
  - Removed render-time `console.log` calls that printed inventory item count and extracted categories while building purchase item category options; save error logs remain.
  - Added a RED/GREEN `dataSafety.test.ts` guard preventing purchase category selector render logs from returning.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager inventory purchase smoke test passed for `/inventory` purchase tab with 0 console errors; the Firestore Listen abort seen after browser close was not a page runtime error.
- Leftover cleanup pass 17 verification:
  - Rechecked employee deletion success debug logging and source history before deletion.
  - Removed the production `console.log` emitted after an employee is marked deleted; employee Firestore tombstone writes, local active-list save, and failure `console.error` handling remain unchanged.
  - Extended the employee deletion safety guard in `dataSafety.test.ts` to prevent the success debug log from returning.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager employees smoke test passed for `/employees` with 0 console errors; the Firestore Listen abort seen after browser close was not a page runtime error.
- Leftover cleanup pass 18 verification:
  - Rechecked loan-management cash-flow, loan, and expense success debug logging before deletion.
  - Removed production `console.log` calls that printed cash-flow sync success, cash-flow payloads, loan sync success, and employee-loan expense payloads; loan, expense, and cash-flow cloud writes remain awaited before local state updates.
  - Extended the employee loan safety guard in `dataSafety.test.ts` to prevent loan-management `console.log` calls from returning.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager loan-management smoke test passed for `/employees/loans` with 0 console errors; the Firestore Listen abort seen after browser close was not a page runtime error.
- Leftover cleanup pass 19 verification:
  - Rechecked salary-settlement success and payload debug logging before deletion.
  - Removed production `console.log` calls that printed salary settlement sync success and salary-expense payloads; salary record, salary expense, loan deduction, and cash-flow writes remain awaited before local state updates.
  - Extended the employee salary safety guard in `dataSafety.test.ts` to prevent salary-settlement `console.log` calls from returning.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager salary-settlement smoke test passed for `/employees/salary` with 0 console errors; the Firestore Listen abort seen after browser close was not a page runtime error.
- Leftover cleanup pass 20 verification:
  - Rechecked supplier-management save and payment success debug logging before deletion.
  - Removed production `console.log` calls that printed supplier save success and supplier-payment expense payloads; supplier edits, new suppliers, supplier payments, purchase-order updates, and expense writes remain awaited before local state updates.
  - Extended the supplier safety guard in `dataSafety.test.ts` to prevent supplier-management `console.log` calls from returning.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager suppliers smoke test passed for `/inventory/suppliers` with 0 console errors; the Firestore Listen abort seen after browser close was not a page runtime error.
- Leftover cleanup pass 21 verification:
  - Rechecked manager data-overview count/debug logging before deletion.
  - Removed production `console.log` calls that printed overview load parameters, filtered order count, expense count, and purchase count; revenue, cash/card, order, expense, purchase, and ranking calculations remain unchanged.
  - Extended the manager dashboard safety guard in `dataSafety.test.ts` to prevent data-overview `console.log` calls from returning; mismatch `console.warn` and failure `console.error` diagnostics remain.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager overview smoke test passed for `/manager` with 0 console errors; Firebase IndexedDB deprecation warnings and the Firestore Listen abort after browser close were not page runtime errors.
- Leftover cleanup pass 22 verification:
  - Rechecked financial-report generation and data-count debug logging before deletion.
  - Removed production `console.log` calls that printed generated report dates, order counts, expense counts, and current report data status; report totals, daily details, order summaries, supplier debt, and print data remain unchanged.
  - Extended the financial report safety guard in `dataSafety.test.ts` to prevent financial-report `console.log` calls from returning; mismatch `console.warn` and failure `console.error` diagnostics remain.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager financial report smoke test passed for `/manager/financial-reports` with 0 console errors; Firebase IndexedDB deprecation warnings and the Firestore Listen abort after browser close were not page runtime errors.
- Leftover cleanup pass 23 verification:
  - Rechecked kitchen-display order-count debug logging before deletion.
  - Removed production `console.log` calls that printed kitchen initial order count and DataManager order update count; kitchen subscription, order filtering, and POS order status write-back remain unchanged.
  - Extended the kitchen status safety guard in `dataSafety.test.ts` to prevent kitchen-display `console.log` calls from returning; failure `console.error` diagnostics remain.
  - Full Jest suite passed: 12 suites, 136 tests.
  - `npx tsc --noEmit` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager kitchen smoke test passed for `/kitchen` with 0 console errors; Firebase IndexedDB deprecation warnings and the Firestore Listen abort after browser close were not page runtime errors.
- Leftover cleanup pass 24 verification:
  - Rechecked remaining services/hooks/utils references and confirmed there were no zero-reference source files safe to delete.
  - Hardened `dataManager` so store-scoped business caches refuse bare `localStorage` reads/writes when `storeId` is missing; this protects orders, customers, expenses, purchases, handovers, employees, menu items, and inventory from falling back to global business keys.
  - Extended `dataSafety.test.ts` to prevent the old no-store fallback from returning.
  - Full Jest suite passed: 12 suites, 137 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for `/pos`, `/manager/financial-reports`, `/employees`, and `/kitchen` with 0 console errors.
- Leftover cleanup pass 25 verification:
  - Rechecked `dataManager.saveData` and found the Firestore branch used `forEach(async ...)`, so callers using `await dataManager.saveData(...)` could continue before cloud writes finished.
  - Changed the Firestore branch to build sync tasks and `await Promise.all(syncTasks)` before notifying module listeners, and rethrow failed sync errors so failed cloud writes are not silent.
  - Extended `dataSafety.test.ts` to prevent non-awaited `data.forEach(async ...)` Firestore sync from returning.
  - Full Jest suite passed: 12 suites, 138 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for `/pos`, `/manager/order-history`, `/manager/financial-reports`, `/employees`, and `/inventory/suppliers` with 0 console errors.
- Leftover cleanup pass 26 verification:
  - Rechecked unawaited cloud-write patterns after the DataManager fix.
  - Hardened kitchen status write-back so item status changes and kitchen completion first wait for the local `orders` cache save, then write the shared `pos_orders` document; failures remain logged in the existing kitchen error path.
  - Extended `dataSafety.test.ts` to prevent kitchen status changes from returning to independent, non-sequenced save/write calls.
  - Full Jest suite passed: 12 suites, 138 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
  - Firebase Hosting deployed to `https://restaurant-pos-1b420.web.app`.
  - Online store-manager smoke test passed for `/kitchen` and `/pos` with 0 console errors.
- POS frontend experience pass verification:
  - Changed complete-order publishing so completed orders are queued to `pos_orders` in the background after local completion state is applied, reducing button blocking on weak network without changing the stock-deduction-on-complete rule.
  - Added a persistent pending-sync marker path for queued completed orders using the existing `pendingOrderSyncIds` mechanism.
  - Polished the POS overview only: table canvas now has lightweight local food-style artwork plus grid background, table cards and order cards have stronger commercial spacing/shadows, and the right order filter puts `全部` beside `订单列表` while the lower row keeps only `堂食`, `打包`, and `外卖`.
  - Extended `dataSafety.test.ts` to lock the non-blocking complete-order publish path and the compact order-filter layout.
  - Targeted POS data-safety test passed: 104 tests.
  - Full Jest suite passed: 12 suites, 139 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS table background image replacement:
  - Replaced the first lightweight line-art background with a local generated food-table JPG asset at `client/src/assets/pos/table-food-background.jpg`.
  - POS imports the local image asset and layers a soft overlay/grid above it so table status colors remain readable and the page does not depend on external image hosting.
  - Updated `dataSafety.test.ts` to prevent returning to embedded SVG line art for the table canvas background.
  - Targeted POS data-safety test passed: 104 tests.
  - Full Jest suite passed: 12 suites, 139 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS Spanish staff-facing labels:
  - Changed POS cashier staff-facing labels, buttons, order filters, order status chips, payment area, customer selector, receipt toolbar, and menu selector text to Spanish.
  - Preserved stored business keys such as `dine_in`, `takeout`, `delivery`, `completed`, and `cancelled`; only UI display text changed.
  - Set the takeout display label to `Barra` per the business requirement.
  - Targeted POS data-safety test passed: 104 tests.
  - Full Jest suite passed: 12 suites, 139 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS table background final image replacement:
  - Replaced `client/src/assets/pos/table-food-background.jpg` with the user-provided food-table image from `C:\Users\华为\Desktop\菜品\ChatGPT Image 2026年6月18日 12_13_02.png`.
  - Kept the image at its original `1586 x 992` dimensions and exported it as JPG at about 264 KB for browser-friendly loading.
  - The existing POS overlay and grid remain unchanged so table status colors stay readable.
- POS pseudo-3D directional table layout:
  - Added persistent table `shape` and `orientation` fields so future table layout saves keep the intended visual shape.
  - Merged tables now use the original tables' bounding box instead of resetting to a fixed block, so horizontal merges render as horizontal long tables and vertical merges render as vertical long tables.
  - Split merged tables still restores each original table's saved position, size, shape, and orientation.
  - Replaced fixed table render height with dynamic width/height and pseudo-3D shadows/highlights.
  - Added a regression guard in `dataSafety.test.ts` for directional merge dimensions and dynamic table rendering.
  - Targeted POS data-safety test passed: 105 tests.
  - Full Jest suite passed: 12 suites, 140 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS table visual correction:
  - Root cause: the first pseudo-3D pass still rendered table surfaces as weakly rounded rectangles, so normal tables still looked like square blocks.
  - Normal single tables now infer an oval tabletop by default, while merged or very long tables keep directional long-table shapes.
  - Added tabletop perspective, oval radial highlights, deeper shadows, and small table legs so the POS canvas reads as tables instead of flat blocks.
  - Reduced the POS food-background white overlay and stopped stretching the background with `cover`, improving image clarity.
  - Added regression guards for oval pseudo-3D tabletop rendering and sharp food-background settings.
  - Targeted POS data-safety test passed: 107 tests.
  - Full Jest suite passed: 12 suites, 142 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS square-table visual correction:
  - Updated the table visual direction from oval tables to square pseudo-3D tables so later merged tables can clearly show horizontal and vertical table shapes.
  - Existing stale `round` table-shape cache is treated as square during normalization/display, preventing old browser cache from keeping round tables.
  - Merged tables still keep directional rectangle sizing from the original table bounding box.
  - Fixed the food background seam by returning the image layer to continuous `cover` sizing while keeping the lighter overlay and explicit no-repeat image layer.
  - Updated regression guards for square tabletop rendering and continuous background behavior.
  - Targeted POS data-safety test passed: 107 tests.
  - Full Jest suite passed: 12 suites, 142 tests.
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run build` passed.
- POS modern table asset correction:
  - Replaced the CSS-drawn table legs and colored block tabletops with real lightweight PNG assets in the selected modern wood-table direction.
  - Added separate assets for single tables, horizontal merged tables, and vertical merged tables so split/merge shape remains visually clear.
  - Table state now changes the table image itself: available tables keep natural color with no green frame, occupied tables tint red, and paid/cleaning tables tint orange.
  - Removed the outer status frame/dot treatment so the POS table canvas stays cleaner and closer to a real table layout.
  - Cleaned the table PNG assets by removing the pale tabletop highlight strip and light metal leg strokes that looked like white bars in the POS canvas.
  - Added regression guards to prevent reintroducing the old CSS block furniture.
  - Targeted POS data-safety test passed: 109 tests.
  - `npm run build` passed.
- POS payment tax/service Spanish labels:
  - Changed the POS order summary from `税费/服务费` to Spanish `IVA (15%)` and `Servicio (10%)`.
  - Changed the POS payment toggles from `税费/服务费` to `IVA` and `Servicio`.
  - Changed the POS receipt-panel time label from Chinese `时间` to Spanish `Hora`.
  - Updated receipt printing to include `Subtotal`, optional `IVA (15%)`, optional `Servicio (10%)`, and final `Total` based on `finalTotal`.
  - Added a regression guard in `dataSafety.test.ts` so the visible POS payment, order summary, and print blocks do not return to Chinese/mojibake service/tax labels.
  - Targeted POS data-safety test passed: 110 tests.
  - `npm run build` passed.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`.

- Manager data overview redesign:
  - Rebuilt `client/src/pages/Manager/Dashboard.tsx` into a professional manager analytics screen with compact KPI cards, manual cloud refresh, monthly sales calendar heatmap, product ranking, beverage ranking, channel filters, and period movement comparison.
  - Added `client/src/utils/dashboardAnalytics.ts` for reusable analytics calculations: date range normalization, collected-order filtering, beverage category fallback from menu/inventory data, ranking by revenue or quantity, monthly Monday-Sunday calendar, KPI comparison, and increased/decreased item movement.
  - Fixed the old beverage-sales blind spot by resolving order item categories from item data, linked menu items, linked inventory items, and item-name matches. Beverage groups now support `Cerveza`, `Bebida`, and `Jugo`.
  - Data overview refresh now pulls low-frequency `menu_items` and `inventory_items` snapshots together with orders/expenses/purchases and saves them through store-scoped `dataManager` keys (`menuItems`, `inventory`) without realtime subscription.
  - Added regression coverage in `client/src/utils/dashboardAnalytics.test.ts` and `client/src/utils/dataSafety.test.ts`.
  - Targeted analytics test passed: 8 tests.
  - Targeted data-safety test passed: 111 tests.
  - `npm run build` passed.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL returned HTTP 200.
- Expense category hierarchy:
  - Redesigned expense categories from a flat list into parent-child accounting categories while keeping the existing `expense_categories` collection and store-scoped local cache.
  - Added `client/src/utils/expenseCategories.ts` for default parent groups, legacy flat-category normalization, child filtering, category path resolution, and delete protection.
  - Updated `ExpenseRecords.tsx` so new expenses select parent category first, then child category, and save `parentCategoryId`, `parentCategoryName`, `categoryId`, and `categoryName` snapshots.
  - Rebuilt category management into a parent list plus child list. Parent deletion is blocked when children exist; child deletion is blocked when historical expenses reference it.
  - Updated daily financial report breakdowns to group and print categories as `父类 / 子类`, while old records still resolve from legacy `categoryId`.
  - Added regression coverage in `expenseCategories.test.ts`, `financeMetrics.test.ts`, and `dataSafety.test.ts`.
  - Targeted tests passed: 129 tests.
  - `npm run build` passed.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL returned HTTP 200.

## Files Changed

- `client/src/pages/Manager/ExpenseRecords.tsx`
- `client/src/pages/Manager/ShiftHandover.tsx`
- `client/src/pages/Manager/OrderHistoryPage.tsx`
- `client/src/utils/orderHistory.ts`
- `client/src/utils/orderHistory.test.ts`
- `client/src/utils/dataSafety.test.ts`
- `client/src/services/smartSyncService.ts`
- `client/src/services/backupExportService.ts`
- `client/src/utils/exchangeRate.ts`
- `client/src/pages/Manager/Stores.tsx`
- `client/src/pages/Manager/ExchangeRateSettings.tsx`
- `client/src/pages/Settings/PermissionsModule.tsx`
- `client/src/types/index.ts`
- `client/scripts/*.cjs`
- `client/test-results/cancel-auth-focus/*`
- `server/*`
- `shared/types/index.ts`
- `client/src/pages/MODULES_OVERVIEW.md`
- `docs/2026-06-15_PROGRESS.md`
- `docs/2026-06-11_EXECUTION_PLAN.md`
- `README.md`
- root historical Markdown documents
- `docs/2026-06-09_HANDOFF.md`
- `docs/2026-06-10_PROGRESS.md`
- `docs/2026-06-14_DAILY_WRAPUP.md`
- `client/src/components/SplitBillModal.tsx`
- `client/src/contexts/AppContext.tsx`
- `client/src/pages/Inventory/FridgeStocktake.tsx`
- `client/src/pages/Inventory/SupplierManagement.tsx`
- `client/src/pages/Manager/ExpenseRecords.tsx`
- `client/src/pages/Manager/ShiftHandover.tsx`
- `client/src/pages/POS/POS.tsx`
- `client/src/utils/dataSafety.test.ts`
- `client/src/services/smartSyncService.ts`
- `client/src/services/dataManager.ts`
- `client/src/pages/Settings/PermissionsModule.tsx`
- `client/src/pages/Login/Login.tsx`
- `client/src/contexts/AuthContext.tsx`
- `client/src/services/FirebaseAuthService.ts`
- `client/src/firebase/index.ts`
- `client/src/pages/WaiterInterface/WaiterInterface.tsx`
- `client/src/pages/Inventory/WarehouseStocktake.tsx`
- `client/src/App.tsx`
- `client/src/pages/Inventory/PurchaseManagement.tsx`
- `client/src/pages/Employees/EmployeeList.tsx`
- `client/src/pages/Employees/LoanManagement.tsx`
- `client/src/pages/Employees/SalarySettlement.tsx`
- `client/src/pages/Inventory/SupplierManagement.tsx`
- `client/src/pages/Manager/Dashboard.tsx`
- `client/src/pages/Manager/FinancialReports.tsx`
- `client/src/pages/POS/KitchenDisplay.tsx`

## Next Queue

1. Continue cleanup only after source-history/import/test checks.
2. Do not delete or rewrite live business data.
3. Next pass, if no production issue is reported first: continue reviewing remaining noisy console logging and mojibake comments only after history checks before any deletion.

## 2026-06-18 Update

- Manager data overview visual redesign, template pass:
  - Reworked `client/src/pages/Manager/Dashboard.tsx` to follow the selected blue-green professional operations dashboard concept instead of the previous plain white/black layout.
  - KPI cards now sit in one top row with compact circular accent badges, softer color hierarchy, and smaller trend pills so the five core metrics can be scanned together.
  - Monthly sales calendar and sales ranking now share the main first-screen area, matching the selected template structure more closely.
  - Calendar values use compact money labels inside cells to avoid clipping when weekly totals are large.
  - Kept the existing analytics, store-scoped data loading, manual cloud refresh, filters, ranking, and comparison logic unchanged.
  - Verified with `npm test -- --watchAll=false --runInBand src/utils/dashboardAnalytics.test.ts src/utils/dataSafety.test.ts`: 120 tests passed.
  - Verified with `npm run build`: production build passed.
  - Checked locally with Playwright login as `zeng/123456`; `/manager` loaded with 0 console errors and 5 KPI cards in one row.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL returned HTTP 200.

## 2026-06-20 Update

- Fixed existing menu item image edits disappearing after refresh:
  - Root cause: manual menu refresh merged local and cloud menu records only by whole-record `lastModified`, so a newer local menu record could block cloud `imageUrl`, `imageStoragePath`, and `imageUpdatedAt` fields from being restored.
  - Updated `client/src/utils/syncMerge.ts` so menu image fields merge independently by `imageUpdatedAt` and completed cloud uploads can replace a same-version local `imageUploadPending` state.
  - Added `client/src/utils/syncMerge.test.ts` covering existing menu records with missing local image fields and pending local image uploads.
  - Verified with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts src/utils/syncMerge.test.ts`: 114 tests passed.
  - Verified with `npm run build`: production build passed.

- System UI redesign planning and Task 1 foundation:
  - Confirmed Product Design direction: admin screens use the professional operations style, owner mobile absorbs the mobile data style, and POS/waiter/kitchen use the front-of-house efficiency style.
  - Added design spec `docs/superpowers/specs/2026-06-20-ui-redesign-design.md` and execution plan `docs/superpowers/plans/2026-06-20-ui-redesign-plan.md`.
  - Added shared visual token files `client/src/styles/uiTokens.ts` and `client/src/styles/adminUi.ts` for later UI-only screen passes.
  - Added a `dataSafety.test.ts` guard so shared UI tokens exist without touching business service behavior.
  - Verified with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts`: 113 tests passed.
  - Verified with `npm run build`: production build passed.

- System UI redesign Task 2, app shell and login polish:
  - Reworked `client/src/pages/Login/Login.tsx` into a responsive two-panel commercial login screen using the shared UI tokens.
  - Reworked `client/src/components/Layout/MainLayout.tsx` into a cleaner admin shell with a polished top bar, sidebar navigation, permission-filtered menu groups, and a styled full-screen module menu for POS/waiter/kitchen routes.
  - Kept login, redirect routing, logout, hidden-sidebar routes, and permission filtering logic intact; only visual structure and text presentation changed.
  - Added a `dataSafety.test.ts` guard for app-shell UI polish so permission routing and login flow markers remain present.
  - Verified with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts`: 114 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked local login page and admin dashboard shell at `http://localhost:3000`: 0 console errors, no Chinese mojibake, screenshots saved under `client/output/playwright/`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 3, front-of-house first pass:
  - Applied shared UI tokens to `client/src/pages/POS/POS.tsx`, `client/src/components/MenuSelection.tsx`, `client/src/pages/WaiterInterface/WaiterInterface.tsx`, and `client/src/pages/POS/KitchenDisplay.tsx`.
  - POS order mode now uses consistent card panels, softer borders, and shared page background while keeping payment, stock deduction, cancel authorization, order publishing, and table state handlers unchanged.
  - POS order list and summary band now share the same operations-card style as the app shell.
  - Menu selection search/category band and menu cards were cleaned up with shared spacing, borders, shadows, and readable image cards while preserving `MenuImage`, search autofocus, and add-item callbacks.
  - Waiter table and order screens now use the shared shell colors and card treatment while keeping POS-synced table layout read-only.
  - Kitchen display now uses shared stat cards, softer order cards, and clearer filter controls while preserving order status writes back to `pos_orders`.
  - Added a `dataSafety.test.ts` guard proving the front-of-house UI pass keeps POS order writes, table subscription, waiter writes, kitchen writes, and `MenuImage` usage intact.
  - Verified RED first: the new guard failed before UI token imports were added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts`: 115 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked `/pos`, `/waiter`, and `/kitchen` after login as `zeng/123456`: pages loaded with text, no mojibake, and only Firebase IndexedDB deprecation warnings.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 4, inventory and menu management first pass:
  - Applied shared UI tokens to `client/src/pages/Inventory/Inventory.tsx` and `client/src/pages/Inventory/MenuManagement.tsx`.
  - Inventory item management now has a cleaner page background, rounded tab controls, compact search/category toolbar, clearer refresh/sync controls, and a polished list shell while keeping inventory save/delete/category cloud writes unchanged.
  - Menu management now has a cleaner header, compact refresh/category/add actions, refined search/category toolbar, softer menu cards, and token-based status badges while keeping menu save/delete/image upload behavior unchanged.
  - Added a `dataSafety.test.ts` guard proving this UI pass keeps `inventory_items`, `inventory_categories`, `menu_items`, `processAndUploadMenuImage`, and `MenuImage` behavior present.
  - Verified RED first: the new guard failed before inventory/menu UI token imports were added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts src/utils/syncMerge.test.ts`: 118 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `zeng/123456`: `/inventory` and `/inventory/menu` loaded with expected controls and screenshots saved as `docs/verification-inventory-ui.png` and `docs/verification-menu-ui.png`.
  - Note: CRA development server still has an unrelated existing image asset hot-reload error for POS table/background assets; production build and Firebase deploy are unaffected.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 5, employee management first pass:
  - Applied shared UI tokens to `client/src/pages/Employees/Employees.tsx`, `client/src/pages/Employees/EmployeeList.tsx`, `client/src/pages/Employees/LoanManagement.tsx`, and `client/src/pages/Employees/SalarySettlement.tsx`.
  - Employee management shell now uses the shared page background, polished sync/refresh header, compact cards, softer table headers, and consistent button hierarchy.
  - Employee list, loan management, and salary settlement cards now share the same operations UI style while keeping employee save/delete, deletion tombstones, loan expense creation, cash-flow writes, salary calculation, and loan deduction writes unchanged.
  - Added a `dataSafety.test.ts` guard proving this UI pass keeps `employees`, `employee_deletions`, `loan_records`, `expenses`, `salary_records`, and salary calculation/write paths present.
  - Verified RED first: the new guard failed before employee UI token imports were added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/employeeRecords.test.ts src/utils/employeeLoans.test.ts src/utils/dataSafety.test.ts`: 120 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `zeng/123456`: `/employees`, `/employees/loans`, and `/employees/salary` loaded with expected controls and screenshots saved as `docs/verification-employees-ui.png`, `docs/verification-employee-loans-ui.png`, and `docs/verification-employee-salary-ui.png`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 6, manager dashboard UI token pass:
  - Applied shared UI tokens to `client/src/pages/Manager/Dashboard.tsx`.
  - Refined the manager dashboard container, filter toolbar, segmented controls, KPI cards, section cards, and table styles to use the same operations UI foundation as the rest of the system.
  - Kept the existing analytics pipeline unchanged: `buildKpis`, `buildSalesRankings`, `buildMonthlySalesCalendar`, `buildRankingComparison`, manual `smartGetDocuments` refresh, and no realtime order subscription.
  - Added a `dataSafety.test.ts` guard proving this UI pass keeps analytics and manual refresh paths intact.
  - Verified RED first: the new guard failed before dashboard UI token import was added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/dashboardAnalytics.test.ts src/utils/dataSafety.test.ts`: 126 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `zeng/123456`: `/manager` loaded with expected KPI, monthly calendar, ranking, and refresh controls; screenshot saved as `docs/verification-manager-dashboard-ui.png`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 7, financial reports UI polish:
  - Applied shared UI tokens to `client/src/pages/Manager/FinancialReports.tsx`.
  - Refined the financial report page container, report filter toolbar, sync badge, KPI cards, report detail table, daily expense/purchase detail table, and floating print button.
  - Kept all financial formulas and data paths unchanged: `calculateFinancialReportTotals`, `calculateOrderStatusSummary`, `buildDailyExpenseBreakdown`, manual `smartGetDocuments` refresh, supplier debt summary, and daily print detail rows remain intact.
  - Added a `dataSafety.test.ts` guard proving this UI pass keeps the formulas, order reads, daily expense grouping, and print detail path present.
  - Verified RED first: the new guard failed before financial report UI token import was added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/financeMetrics.test.ts src/utils/dataSafety.test.ts`: 131 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `zeng/123456`: `/manager/financial-reports` loaded with report KPI cards, report detail table, daily detail section, and print button; screenshot saved as `docs/verification-financial-reports-ui.png`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 8, settings and permissions UI polish:
  - Applied shared UI tokens to `client/src/pages/Settings/PermissionsModule.tsx`, `client/src/pages/Settings/DataBackup.tsx`, `client/src/pages/Manager/Stores.tsx`, and `client/src/pages/Manager/ExchangeRateSettings.tsx`.
  - Refined role cards, permission workspace, backup export cards, store list/detail panels, exchange-rate form, refresh/sync badges, buttons, tables, and modal shells.
  - Kept all settings data paths unchanged: `system_roles` role writes, backup export/download, single-document store writes/deletes, user writes/deletes, and store-scoped exchange-rate writes remain intact.
  - Added a `dataSafety.test.ts` guard proving this UI pass keeps shared tokens and the key settings data paths present.
  - Verified RED first: the new guard failed before settings UI token imports were added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/dataSafety.test.ts`: 120 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `admin/admin123`: `/settings/permissions`, `/settings/backup`, `/settings/stores`, and `/settings/exchange-rate` loaded with expected controls; screenshots saved as `docs/verification-settings-permissions-ui.png`, `docs/verification-settings-backup-ui.png`, `docs/verification-settings-stores-ui.png`, and `docs/verification-settings-exchange-ui.png`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- System UI redesign Task 9, owner dashboard mobile polish:
  - Applied shared UI tokens to `client/src/pages/Dashboard/OwnerDashboard.tsx`.
  - Refined owner dashboard card, filter, panel, store performance, alert, and chart styling with softer borders and a mobile-first 520px breakpoint.
  - Fixed the app shell mobile layout in `client/src/components/Layout/MainLayout.tsx`: narrow screens now hide the sidebar and use the existing fullscreen menu, so the owner dashboard is not squeezed on phones.
  - Kept owner aggregation paths unchanged: global `stores` read, per-store collection reads, `dedupeOwnerRecordsById`, local owner cache, payment breakdown, and financial date handling remain intact.
  - Added a `dataSafety.test.ts` guard proving the owner dashboard keeps aggregation reads and the shell keeps the narrow-screen fullscreen menu behavior.
  - Verified RED first: the new guard failed before owner dashboard UI token import was added.
  - Verified GREEN with `npm test -- --watchAll=false --runInBand src/utils/ownerDashboardData.test.ts src/utils/dataSafety.test.ts`: 125 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `admin/admin123`: `/dashboard` loaded in desktop and mobile viewports; mobile sidebar is hidden and metric card width is 370px at a 390px viewport. Screenshots saved as `docs/verification-owner-dashboard-ui.png` and `docs/verification-owner-dashboard-mobile-ui.png`.
  - Deployed to Firebase Hosting project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app` returned HTTP 200.

- Canada dry inventory discrepancy investigation and POS stock idempotency fix:
  - Investigated live Firestore data for store `store_1776725610354` using read-only Firebase queries as user `zeng`.
  - Evidence: fridge stocktake for `Canada dry` on Nicaragua date `2026-06-19` recorded actual/system stock 3 in `2号冰箱`, warehouse 0; the next stocktake on `2026-06-20` recorded fridge system stock -1 and warehouse system stock -1 before correction.
  - Evidence: Nicaragua date `2026-06-20` completed Canada dry sales were exactly order `0620074` quantity 2 and order `0620088` quantity 1, total 3. The order item quantities themselves were correct.
  - Root cause: POS completion checked only local `order.stockDeducted` and localStorage before decrementing stock. It then decremented inventory first and wrote `stockDeducted` back to Firestore later through async order publishing, leaving a multi-device/network-delay window where the same order could be completed again and deduct inventory twice.
  - Added `smartClaimOrderStockDeduction` in `client/src/services/smartSyncService.ts`, using Firestore `runTransaction` to claim an order-level stock deduction lock before any inventory decrement.
  - Updated `client/src/pages/POS/POS.tsx` so `deductStockForOrder` claims the cloud order before calling `deductStock(itemsToDeduct)`, skips if the cloud order is already deducted, and blocks duplicate in-progress deductions from another device.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving POS claims the order in Firestore before decrementing inventory.
  - Verified RED first: the new guard failed before the transaction claim existed.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 122 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app`.

- Owner dashboard branch selector, fixed modules, and invoice evidence view:
  - Root cause for doubled owner dashboard totals: the global `stores` query could return duplicate logical stores where an old document had a different Firestore document id but the same data `id`; the dashboard looped raw `loadedStores` before dedupe and therefore read `stores/{sameStoreId}/pos_orders` twice.
  - Updated `client/src/pages/Dashboard/OwnerDashboard.tsx` so `loadedStores` is deduped into `activeStores` before reading each store's subcollections.
  - Added `selectedStoreId = 'all'` plus a mobile-friendly branch dropdown. The dashboard keeps fixed modules on screen and switches their data scope when selecting `全部分店`, `Bluefields`, `Managua`, or any later store.
  - Reworked the owner KPI cards to fixed business modules: revenue, orders, cash, card, operating expense, purchase expense, profit/loss, and supplier payable.
  - Added owner-side `开支/采购发票` evidence list and modal image preview. It reads existing manager expense receipt fields (`receipt`, `receiptImage`) and purchase invoice image fields (`invoiceImage`, `imageUrl`) without adding any new upload path.
  - Added `dedupeOwnerRecordsByStoreAndId` and `buildOwnerExpenseEvidenceRows` in `client/src/utils/ownerDashboardData.ts` so owner records are deduped per store and expense/purchase evidence rows are testable.
  - Added regression guards in `client/src/utils/ownerDashboardData.test.ts` and `client/src/utils/dataSafety.test.ts` proving duplicate store reads are prevented, fixed modules are scoped by selected store, and invoice/receipt evidence remains visible.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/ownerDashboardData.test.ts`: 6 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 123 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview and deployed site after login as `admin/admin123`: owner dashboard showed 8 KPI cards, branch dropdown options `全部分店`, `Bluefields`, `Managua`, and the `开支/采购发票` panel. Screenshots saved under `output/playwright/owner-dashboard-*.png`.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`; live URL `https://restaurant-pos-1b420.web.app`.

- Owner dashboard scroll clipping fix:
  - Reproduced the deployed issue with Playwright on mobile and desktop: `.owner-page` had thousands of pixels of content, but the app shell `<main>` had `overflow: hidden`, so the document height stayed equal to the viewport and neither phone nor desktop could scroll down.
  - Updated `client/src/components/Layout/MainLayout.tsx` to allow page-level scrolling for `/dashboard` only: `overflowY: auto`, `WebkitOverflowScrolling: touch`, and `minHeight: 0` inside the flex shell. POS, kitchen, and waiter full-screen surfaces keep their previous hidden-overflow behavior.
  - Added a `dataSafety.test.ts` guard proving the owner dashboard route keeps an app-shell scroll path.
  - Verified RED first: the new guard failed before the layout scroll change.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 124 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `admin/admin123`: mobile main scroll moved from 0 to 2664, desktop main scroll moved from 0 to 1203, proving the lower dashboard content is reachable. Screenshots saved as `output/playwright/owner-dashboard-scroll-mobile.png` and `output/playwright/owner-dashboard-scroll-desktop.png`.

- Owner dashboard employee count and polluted employee/store data cleanup:
  - Reproduced the mismatch with live Firestore reads as `admin/admin123`.
  - Root cause: Bluefields had 11 raw employee docs, but 4 were deleted employees recorded in `employee_deletions`, and 2 were old auto-id duplicate employee docs whose internal `id` matched deleted employees. Employee management filtered them correctly; owner dashboard did not read `employee_deletions`, so it displayed 7 instead of 5.
  - Updated `client/src/pages/Dashboard/OwnerDashboard.tsx` to read each store's `employee_deletions` subcollection and reuse the existing `filterActiveEmployees` helper for owner employee counts. This keeps one employee filtering rule instead of duplicating logic.
  - Deleted confirmed polluted cloud docs:
    - `stores/SbGWwKxjabIkDcGbypKq` duplicate empty Bluefields store shell.
    - `stores/store_1776725610354/employees/HgjhlZRNzvnoeXEZxUgP` old duplicate employee doc for deleted employee `11`.
    - `stores/store_1776725610354/employees/rSN9SD62kFbgbIlBaMfw` old duplicate employee doc for deleted employee `bekiy`.
  - Verified after cleanup: cloud stores are now `Bluefields` and `Managua`; Bluefields raw employees 9, deletion records 4, active employees 5 (`Rafa`, `Adriana Montiel`, `BELKI`, `ADRIANA`, `MARGARITA`).
  - Added a `dataSafety.test.ts` guard proving owner dashboard employee counts must use `employee_deletions` plus `filterActiveEmployees`.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/employeeRecords.test.ts`: 1 test passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 125 tests passed.
  - Verified with `npm run build`: production build passed.

- Owner dashboard sales product ranking:
  - Added `menu_items` to owner dashboard per-store reads and owner local cache so product rankings can resolve menu categories consistently with manager data overview.
  - Reused the existing manager analytics helpers from `client/src/utils/dashboardAnalytics.ts`: `buildSalesRankings`, `buildRankingComparison`, `filterOrdersByRange`, and `normalizeDashboardRange`. No duplicate ranking algorithm was added.
  - Added a `销售商品排行` panel to `client/src/pages/Dashboard/OwnerDashboard.tsx`, scoped by the existing owner branch selector and time tabs. Filters include all/dishes/beverages, amount/quantity, dine-in/Barra/Delivery, Top 10/20/50, and Cerveza/Bebida/Jugo when beverage scope is selected.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving owner dashboard must read `menu_items` and reuse the manager ranking analytics helpers.
  - Verified RED first: the new guard failed before owner ranking imports and menu reads existed.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 126 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dashboardAnalytics.test.ts`: 8 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `admin/admin123`: `/dashboard` showed `销售商品排行`, filter controls, and 10 ranking rows. Screenshot saved as `output/playwright/owner-dashboard-ranking-local.png`.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site after login as `admin/admin123`: `https://restaurant-pos-1b420.web.app/dashboard` showed 10 ranking rows, filter controls, and no browser console errors. Screenshot saved as `output/playwright/owner-dashboard-ranking-live.png`.

- Owner dashboard mobile sales ranking visibility fix:
  - Reproduced the phone symptom with an iPhone 13 viewport against the deployed site. The ranking data existed in the DOM and had 10 rows, but the title was about 2114px below the initial viewport because mobile KPI cards were one column and the ranking panel was placed after branch cards and the sales trend. The page body itself did not scroll; the app shell internal `main` scroll area did.
  - Updated `client/src/pages/Dashboard/OwnerDashboard.tsx` so narrow screens keep KPI cards in two columns and place the `销售商品排行` panel first inside the mobile content column via `.ranking-panel { order: -1; }`.
  - Fixed a first-pass class placement mistake by tightening the regression guard: `className="panel ranking-panel"` must be attached directly to the sales ranking panel, near the `销售商品排行` title.
  - Added a `dataSafety.test.ts` guard proving owner dashboard keeps the sales ranking panel reachable on narrow mobile screens.
  - Verified RED first: the new mobile reachability guard failed before the ranking panel had the mobile class/order behavior.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 127 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dashboardAnalytics.test.ts`: 8 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview with an iPhone 13 viewport after login as `admin/admin123`: `销售商品排行` exists, 10 ranking rows render, KPI grid is two columns, and no browser console errors. Screenshot saved as `output/playwright/owner-dashboard-ranking-mobile-local-fixed.png`.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site with an iPhone 13 viewport after login as `admin/admin123`: `销售商品排行` exists, 10 ranking rows render, KPI grid is two columns, and no browser console errors. Screenshot saved as `output/playwright/owner-dashboard-ranking-mobile-live-fixed.png`.

- Inventory stock movement cleanup and purchase order date filtering:
  - Root cause for wrong stock movement records: `Inventory.tsx` still returned two hardcoded legacy demo rows (`大米` in, `可乐` out) when the store-scoped `inventory_stock_records` cache was empty. Old browser caches could also keep those demo ids.
  - Updated `client/src/pages/Inventory/Inventory.tsx` so stock movement records no longer fall back to demo data, and local cached legacy demo ids `rec1`/`rec2` are filtered out.
  - Added an empty state `暂无真实出入库记录` so the UI clearly shows there are no real records instead of showing fake movements.
  - Updated `client/src/pages/Inventory/PurchaseManagement.tsx` so purchase order filters default to Nicaragua local today, reset returns to today, filtered lists sort newest first, and all purchase/received dates go through safe date formatting instead of rendering `Invalid Date`.
  - Added regression guards in `client/src/utils/dataSafety.test.ts` for the legacy stock movement cleanup and purchase order date behavior.
  - Verified RED first: both new guards failed before implementation.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 129 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked production build preview after login as `zeng/123456`: `/inventory` stock movement tab showed no `大米`/`可乐`, showed `暂无真实出入库记录`, and purchase order tab defaulted both date fields to `2026-06-21` Nicaragua local date with no `Invalid Date`.

- Purchase order submit duplicate-click lock:
  - Root cause: `submitPurchaseOrder` had no synchronous in-flight guard and the submit button stayed clickable while purchase order, inventory increment, expense, and supplier cloud writes were pending. On weak networks, repeated clicks could create duplicate purchase orders and duplicate stock increments.
  - Updated `client/src/pages/Inventory/PurchaseManagement.tsx` with an immediate `isSubmittingPurchaseOrderRef` lock plus visible `isSubmittingPurchaseOrder` button state.
  - Disabled the submit button during pending writes and changed the label to `提交中...`.
  - Kept the existing data safety order: cloud writes still finish before local state updates and modal close.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving purchase submit is locked while cloud writes are pending.
  - Verified RED first: the new guard failed before the lock existed.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts -t "purchase order submit"`: 1 targeted test passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 135 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site: `https://restaurant-pos-1b420.web.app/dashboard` loaded with 0 browser console errors. No live purchase order was created during verification.
  - Detailed archive: `docs/2026-06-22_PURCHASE_SUBMIT_LOCK.md`.

- Fridge transfer warehouse cache fix:
  - Root cause: the fridge transfer screen did call warehouse deduction through `smartIncrementField`, but `smartIncrementField` returned after cloud success without updating the store-scoped local cache. The same fallback path only updated existing local docs and could drop missing increment target docs.
  - Updated `client/src/services/smartSyncService.ts` so cloud increment success immediately applies the same increment to local cache before returning, and weak-network fallback uses the same helper.
  - Missing local increment targets are now appended instead of silently leaving local warehouse or fridge cache stale.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving smart increments keep local cache aligned after cloud writes and append new increment docs.
  - Verified RED first: the new guard failed before the `smartIncrementField` local-cache update.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts -t "smart increment keeps local cache"`: 1 targeted test passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 136 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site: `https://restaurant-pos-1b420.web.app` loaded to login with 0 browser console errors. Screenshot saved as `output/playwright/fridge-transfer-cache-fix-live.png`.
  - No live fridge transfer was performed during verification because it would mutate production inventory.
  - Detailed archive: `docs/2026-06-22_FRIDGE_TRANSFER_CACHE_FIX.md`.

- Purchase order `0408` duplicate live-data repair:
  - User reported the vegetable purchase order was duplicated and causing wrong purchase expense totals.
  - Added `client/scripts/repairPurchaseOrderDuplicates.mjs` as a dry-run-first Firestore repair utility; it requires `--apply` before mutating data.
  - Dry-run found Bluefields (`store_1776725610354`) had 10 identical cash purchase orders for order number `0408`, supplier `蔬菜`, amount `C$3015`, plus 10 matching purchase expenses.
  - Applied repair: kept `po-1782170574339` and `purchase_1782170574339`; deleted 9 duplicate purchase orders and 9 duplicate purchase expenses.
  - Reversed the 9 duplicate inventory incoming-stock writes: `6904509823789 -378`, `6907940756314 -225`, `6907236397672 -90`, `6900416486047 -108`, `6908261426173 -225`.
  - Recalculated supplier `sup-1781323756007` balance to `0`.
  - Post-repair verification: Bluefields order `0408` has 1 purchase order, 1 matching purchase expense, expense total `C$3015`; Managua has 0 matching order/expense records.
  - Detailed archive: `docs/2026-06-22_PURCHASE_0408_DUPLICATE_REPAIR.md`.

- POS empty order return button:
  - Fixed the POS ordering empty state where staff could enter a table order, select no dishes, and have no visible way back to the main table/order screen.
  - Added a `← Volver` button in the `Sin pedido` empty order panel. It only resets local POS UI selection state and returns to `overview`; it does not write cloud data, create orders, or touch inventory/payment logic.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving the empty order state keeps a visible return action and resets selected order/table state.
  - Verified RED first: the targeted guard failed before the empty-state marker and button existed.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts -t "POS empty order state keeps"`: 1 targeted test passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 137 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site after login as `zeng`: selected a table, skipped customer selection, saw `Sin pedido` plus `← Volver`, clicked it, returned to `Mesas` / `Pedidos`, and order count stayed unchanged. Browser console showed 0 errors.
  - Detailed archive: `docs/2026-06-22_POS_EMPTY_ORDER_RETURN.md`.

- POS cancelled order changing-time fix:
  - User reported a cancelled order whose displayed time kept changing to the current time.
  - Live audit found Bluefields order `order-1782184672149-bqkbn459o`: status `cancelled`, no order number, no table, total `C$0.00`, `createdAt` missing, `cancelledAt` Nicaragua local `2026-06-22 21:55:50`, mutable `lastModified` Nicaragua local `2026-06-22 23:44:58`.
  - Root cause: POS order cards displayed time with `createdAt || preparingAt || completedAt || lastModified`, so malformed cancelled orders fell back to mutable sync metadata.
  - Updated `client/src/pages/POS/POS.tsx` with `getOrderListTimeValue(order)`: cancelled orders now use `cancelledAt` first for filtering, sorting, and display.
  - Added `serializeDateForFirestore(value)` and routed POS order date serialization through it so invalid legacy `Date` objects no longer throw `RangeError: Invalid time value` during sync.
  - Added regression guards in `client/src/utils/dataSafety.test.ts` for cancelled-order display time and invalid date serialization.
  - Verified GREEN with targeted tests for `cancelled order list time` and `invalid Date objects`.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 139 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed POS after login as `zeng/123456`; using a page-clock mock for `2026-06-22 23:58` because real Nicaragua time had crossed midnight, the affected order displayed `Cancelado` and `Pedido 21:55`, with 0 browser console errors.
  - Detailed archive: `docs/2026-06-23_POS_CANCELLED_ORDER_TIME_FIX.md`.

- POS return, status color, and weak-network completion UX:
  - User reported POS order/table colors were inconsistent after confirming an order and returning to the main screen, and later clarified that unconfirmed selected dishes should be cleared when returning to prevent accidental carry-over.
  - Added a shared POS return flow: `returnToOverviewFromOrder` discards only unsent/unconfirmed temporary items, keeps confirmed sent items, and resets local POS selection/payment UI state.
  - Wired the order-screen return buttons to the shared flow so staff can return from an unconfirmed selection and start cleanly.
  - Fixed table image status-color priority so active unpaid tables stay red even if selected, paid awaiting-clear tables stay orange, and available tables use the natural image.
  - Replaced completion/clear-table result `alert()` calls with a non-blocking POS toast so weak-network completion feedback does not trap the cashier behind a browser modal.
  - Added archive notes for the offline/time rule: business timestamps must be captured at the actual restaurant action time in Nicaragua local context and must not be replaced by later cloud sync retry time.
  - Added regression guards for unconfirmed return cleanup, non-blocking completion feedback, and table/order-card status color alignment.
  - Verified RED first with targeted guards.
  - Verified GREEN with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts -t "POS returning from an unconfirmed order|POS table and order card colors|POS completion feedback|POS table status changes the table image color"`: 4 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 142 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site: login page loaded with 0 console errors; after login as `zeng/123456`, `/pos` rendered `Mesas` and `Pedidos`. Screenshot saved as `output/playwright/pos-return-color-offline-live-pos.png`.
  - Browser console on POS showed 2 existing weak-network fallback logs for `menu_items` and `inventory_items`; POS still loaded through local cache.
  - Detailed archive: `docs/2026-06-23_POS_RETURN_COLOR_OFFLINE_UX.md`.

- Purchase order receipt upload removal:
  - User decided to remove the receipt upload interface from Inventory -> Purchase Order Management because the purchase receipt image upload did not display reliably and duplicated the Expense Records receipt flow.
  - Updated `client/src/pages/Inventory/PurchaseManagement.tsx` to remove `invoiceImage`, `fileInputRef`, `handleInvoiceUpload`, and the new-purchase-modal file input/preview block.
  - Kept purchase order submit, inventory increments, supplier balances, purchase expense creation, order number, notes, and payment type unchanged.
  - Expense Records remains the single official receipt/invoice evidence upload and display path.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` proving purchase orders no longer keep a separate image upload entry while Expense Records still keeps receipt upload support.
  - Verified with targeted test: 1 test passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 143 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site after login as `zeng/123456`: Inventory -> Purchase Inbound -> New Purchase Order showed supplier, invoice/order number, payment type, item list, notes, cancel, and submit; no receipt/image upload input or preview remained. Browser console showed 0 errors.
  - Detailed archive: `docs/2026-06-23_PURCHASE_RECEIPT_UPLOAD_REMOVAL.md`.

- Supplier Management module:
  - Moved Supplier Management to canonical route `/suppliers`; old `/inventory/suppliers` was removed because the system is not yet commercially live.
  - Store-manager access now uses `suppliers:manage`; old `inventory:suppliers` permission compatibility was removed.
  - Added supplier account helpers so supplier debt is derived from purchase orders, not stale `supplier.balance`.
  - Removed stale `supplier.balance` from Supplier Management debt display and filters; UI debt now comes from purchase-order-derived helper summaries.
  - Replaced old per-supplier local payment cache paths with one store-scoped `supplier_payments` collection.
  - Linked supplier repayment records to purchase orders and expense records with `supplierPaymentId` and `purchaseOrderId`.
  - Added four page sections: Supplier Profiles, Debt Orders, Payment Records, and Reconciliation Print.
  - Added regression guards in `client/src/utils/dataSafety.test.ts` and helper tests in `client/src/utils/supplierAccounts.test.ts`.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/supplierAccounts.test.ts`: 3 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 149 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site after login as `zeng/123456`: `/suppliers` loaded, all four sections were visible, and browser console error count was 0.
  - Detailed archive: `docs/2026-06-24_SUPPLIER_MANAGEMENT_MODULE.md`.

- Supplier Management permission visibility fix:
  - Root cause: the Permission Management tree still had the old `inventory:suppliers` node, and old browser/cloud `system_roles` configs could override defaults without the new `suppliers:manage` permission.
  - Removed the old Inventory child permission from Permission Management and added `suppliers:manage` as an independent permission item.
  - Added versioned legacy permission migration so old store-manager role configs gain the new supplier module once, while future manual permission removals remain respected.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/permissions.test.ts`: 2 tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts`: 149 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed site: `zeng/123456` with legacy local role cache shows Supplier Management; `admin/admin123` Permission Management shows Supplier Management as an independent item.
  - Detailed archive: `docs/2026-06-24_SUPPLIER_PERMISSION_VISIBILITY_FIX.md`.

- Supplier Management legacy cleanup:
  - Deleted the old supplier component and helpers so future work cannot accidentally use the previous Inventory-based implementation.
  - Removed files: `client/src/pages/Inventory/SupplierManagement.tsx`, `client/src/utils/supplierAccounts.ts`, `client/src/utils/supplierAccounts.test.ts`.
  - Updated supplier safety tests to target the new canonical module under `client/src/pages/Suppliers/`.
  - Added a regression guard proving the old files no longer exist and `App.tsx` imports only `./pages/Suppliers/SupplierWorkbench`.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/utils/dataSafety.test.ts -t supplier`: 9 supplier tests passed.
  - Verified with `npm test -- --watchAll=false --runTestsByPath src/pages/Suppliers/supplierLedger.test.ts`: 5 tests passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed `/suppliers`: new supplier module markers were present, old four-section labels were absent, default month range was `2026-06-01` to `2026-06-30`, and browser console error count was 0.
  - Detailed archive: `docs/2026-06-24_NEW_SUPPLIER_MODULE.md`.

- Customer Management module split:
  - Moved Customer Management out of Store Manager Management into a first-level low-frequency module below Supplier Management.
  - New route: `/customers`; old route `/manager/customers` removed.
  - New permission: `customers:manage`; old default `manager:customers` removed with versioned migration for old role configs.
  - Moved source from `client/src/pages/Manager/CustomersModule.tsx` to `client/src/pages/Customers/CustomersModule.tsx` and deleted `client/src/pages/Manager/ManagerCustomers.tsx`.
  - Kept existing customer cloud reads, deletion tombstones, points settings, and store-scoped local cache behavior unchanged.
  - Verified with targeted data-safety tests, customer records/points/permissions tests, and production build.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed `/customers` after login as `zeng/123456`: Customer Management opened as an independent page, old `/manager/customers` path was absent from rendered HTML, and browser console error count was 0.
  - Detailed archive: `docs/2026-06-24_CUSTOMER_MODULE_SPLIT.md`.

- Customer Center redesign:
  - Rebuilt the independent `/customers` module as a commercial Customer Center instead of copying the old manager-page table.
  - Added `client/src/utils/customerAnalytics.ts` and `client/src/utils/customerAnalytics.test.ts`.
  - Customer analytics now derives spend, visit count, average ticket, last visit, customer segment, and redeem value from current customer records plus paid orders and points transactions.
  - Added KPI strip, segment filters, searchable/sortable customer list, customer 360 detail panel, points settings, and recent points ledger.
  - Preserved strict customer data fields; no compatibility loosening for old customer records because there were no real legacy customers to preserve.
  - Preserved existing customer storage paths and write order: `customers`, `customer_deletions`, `points_transactions`, and `exchange_rate`.
  - Kept low-frequency behavior: manual refresh only, no customer realtime subscription.
  - Verified with `customerAnalytics.test.ts`: 4 tests passed.
  - Verified with targeted `dataSafety.test.ts`: customer center marker/path guard passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed `/customers` after login as `zeng/123456`: customer center, KPI strip, segment filters, detail panel, and points ledger rendered; browser console error count was 0.
  - Screenshot: `client/output/customer-center-redesign-live.png`.
  - Detailed archive: `docs/2026-06-24_CUSTOMER_CENTER_REDESIGN.md`.

- Manager dashboard expense analytics:
  - Added professional expense ranking and share analytics to Store Manager -> Data Overview.
  - Purchase-related expenses are grouped by supplier or purchase order fallback; operating expenses are grouped by expense parent / child category.
  - Added current-period amount, count, average amount, amount share, and previous equal-length period movement.
  - Added filters for all/operating/purchase, amount/count, and Top 10/20/50.
  - Dashboard now manually refreshes `expense_categories` and caches them with store-scoped `dataService.getStoreKey('expense_categories')`; no realtime subscription was added.
  - Replaced the old temporary `expenseByCategory` dashboard list with three panels: expense ranking/share, expense movement, and purchase item Top 10.
  - Verified with `dashboardAnalytics.test.ts`: 10 tests passed.
  - Verified with targeted `dataSafety.test.ts`: manager dashboard expense analytics guard passed.
  - Verified with `npm run build`: production build passed.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked local production build and deployed site after login as `zeng/123456`: `开支排行与占比`, `开支变化`, and `采购物品 Top 10` rendered; browser console error count was 0.
  - Screenshots: `client/output/playwright/manager-expense-analytics-local.png`, `client/output/playwright/manager-expense-analytics-live.png`.
  - Detailed archive: `docs/2026-06-24_MANAGER_EXPENSE_ANALYTICS.md`.

- Manager dashboard layout redesign:
  - Reorganized Store Manager -> Data Overview into the requested five top cards: Revenue, Orders, Sales, Expense, and Profit/Loss.
  - Revenue card now includes cash/card amounts; Orders card now includes dine-in, Barra, and Delivery counts.
  - Removed the separate middle business-type analysis block to free page space.
  - Grouped the rest of the page into Sales Analysis, Current-vs-Previous Period, Expense Analysis, and Customer Analysis.
  - Placed sales movement and expense movement side by side for clearer period comparison.
  - Kept dashboard data loading behavior unchanged: manual refresh only, no new realtime subscription, and existing store-scoped reads remain in place.
  - Added/updated layout regression guard in `client/src/utils/dataSafety.test.ts`.
  - Verified with targeted `dataSafety.test.ts`: 2 selected manager dashboard layout/expense tests passed.
  - Verified with `dashboardAnalytics.test.ts`: 10 tests passed.
  - Verified with `npm run build`: production build passed.
  - Browser-checked local production build after login as `zeng/123456`: five KPI cards and grouped analysis sections rendered; browser console error count was 0.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Browser-checked deployed `/manager` after login as `zeng/123456`: five KPI cards and grouped analysis sections rendered; browser console error count was 0.
  - Screenshots: `client/output/playwright/manager-dashboard-layout-local.png`, `client/output/playwright/manager-dashboard-layout-live.png`.
  - Detailed archive: `docs/2026-06-24_MANAGER_DASHBOARD_LAYOUT_REDESIGN.md`.

- Panda logo replacement:
  - Replaced the old logo mark with a panda SVG in `client/src/logo.svg`.
  - Updated the login screen and main app shell to render the panda logo image instead of the old text badge.
  - Regenerated `favicon.ico`, `logo192.png`, and `logo512.png` as panda browser/PWA icons.
  - Added a regression guard in `client/src/utils/dataSafety.test.ts` to prevent reverting to the old mark.
  - Verified with targeted logo test, production build, local browser check, Firebase Hosting deploy, and live deployed browser check.
  - Screenshots: `client/output/playwright/panda-logo-login-local.png`, `client/output/playwright/panda-logo-main-local.png`, `client/output/playwright/panda-logo-main-live.png`.
  - Detailed archive: `docs/2026-06-24_PANDA_LOGO_REPLACEMENT.md`.

- Owner dashboard order and expense analytics:
  - Added Mesa / Barra / Delivery split inside the owner dashboard Orders metric card.
  - Added store-scoped `expense_categories` loading to owner dashboard manual refresh.
  - Added an Expense Ranking panel below Cost Summary using the same expense ranking helper as manager data overview.
  - Removed the old Reminder panel and its alert calculation/styles.
  - Added regression coverage in `ownerDashboardData.test.ts` and `dataSafety.test.ts`.
  - Verified targeted tests, production build, local browser rendering, Firebase deploy, and live browser rendering after `admin/admin123` login.
  - Screenshots: `client/output/playwright/owner-dashboard-orders-expense-local.png`, `client/output/playwright/owner-dashboard-orders-expense-live.png`.
  - Detailed archive: `docs/2026-06-24_OWNER_DASHBOARD_ORDER_EXPENSE.md`.

- POS terminal order sync retry fix:
  - Fixed a silent pending-sync bug where device A could complete or clear an order locally while the cloud write had actually fallen back to local pending storage.
  - `smartUpdateDocument` now returns explicit cloud-synced, pending, offline, and weak-network fallback status.
  - POS order publishing no longer clears `pendingOrderSyncIdsRef` or marks an order signature as published when the cloud write is pending or failed.
  - Pending local updates now schedule automatic retries: first after 3 seconds, then every 10 seconds while pending changes remain.
  - Verified targeted POS/smart-sync regression tests and production build.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Detailed archive: `docs/2026-06-24_POS_ORDER_TERMINAL_SYNC_RETRY.md`.

- Fridge transfer audit guard:
  - Investigated the reported `TOÑA VIDRIO` mismatch. Cloud stock had already been manually corrected; the observed `102` value matched `COCA VIDRIO`, but the transfer code path could still create duplicate transfers.
  - Replaced simple fridge transfer split increments with `smartTransferFridgeStock`, a single Firestore transaction that updates warehouse stock, fridge stock, and `stock_transfer_records` together.
  - Added cloud stock guards so warehouse-to-fridge cannot run when warehouse stock is insufficient, and fridge-to-warehouse cannot run when fridge stock is insufficient.
  - Added one-click submit protection in the fridge transfer modal: repeated clicks are ignored and the confirm button shows a processing state.
  - Current offline policy for fridge transfer: block official transfer while offline and ask the user to retry online, because this stock movement must verify current cloud stock.
  - Added a follow-up plan to apply the same submit guard to all high-risk write buttons across POS, purchase, stocktake, supplier, and delete flows.
  - Verified targeted `dataSafety.test.ts` fridge-transfer tests and production build.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Detailed archive: `docs/2026-06-25_FRIDGE_TRANSFER_AUDIT_GUARD.md`.

- Attendance check-in state fix:
  - Fixed the attendance mismatch where some employees showed an "already checked in" alert while the button still showed pending and no record appeared for the selected attendance date.
  - Aligned check-in save logic with the attendance screen's `selectedDate` instead of always using the current system date.
  - Existing daily attendance rows without `checkIn` can now be filled in place, while true duplicate check-ins are still blocked.
  - New attendance record ids are deterministic by employee and date.
  - Preserved awaited single-document attendance writes; no bulk overwrite or realtime behavior was added.
  - Adjusted employee attendance printout to A4 portrait, one employee per page, with the latest 15 attendance records fitting on one sheet.
  - Changed attendance printout labels and employee-facing print text from Chinese to Spanish, with print date formatted by `es-NI`.
  - Changed attendance management action buttons and the side navigation label to Spanish, including `Asistencia`, `Marcar entrada`, `Marcar salida`, `Descanso`, `Ausente`, and `Imprimir asistencia`.
  - Verified targeted attendance regression tests and production build.
  - Detailed archive: `docs/2026-06-25_ATTENDANCE_CHECKIN_STATE_FIX.md`.

- Purchase expense to finance link fix:
  - Fixed cash purchase orders not reliably appearing in Expense Records and Financial Reports on the current device.
  - Cash purchase expense records now use deterministic ids and `smartSetDocument('expenses', purchaseExpense.id, purchaseExpense)`.
  - Added stable finance linkage fields: `type: 'purchase'`, `relatedType: 'purchase'`, `purchaseOrderId`, `orderId`, supplier info, and order number.
  - Purchase submit now updates local `purchases` and `expenses` through `dataManager.saveData` after cloud writes succeed.
  - Expense Records listens for `expensesUpdated`; Financial Reports listens for `expensesUpdated` and `purchasesUpdated`.
  - Added historical repair on Expense Records and Financial Reports refresh: old paid cash purchase orders missing expense records are backfilled once; credit purchases remain supplier debt until repayment.
  - Preserved supplier debt rule: credit purchases do not create purchase expenses until repayment.
  - Verified targeted purchase/financial regression tests and production build.
  - Detailed archive: `docs/2026-06-25_PURCHASE_EXPENSE_FINANCE_LINK_FIX.md`.

- Stocktake history modal size consistency:
  - Updated Warehouse Stocktake history modal to match the already-expanded Fridge Stocktake history modal.
  - Warehouse history now uses a 96vw / 1320px / 90vh large modal, compact wrapped header controls, body-level vertical scrolling, and horizontal-only table overflow.
  - Removed the warehouse history table's small nested `300px` scroller.
  - Added regression coverage so both warehouse and fridge stocktake history modals stay large and content-friendly.
  - Verified targeted stocktake history tests and production build.
  - Detailed archive: `docs/2026-06-25_STOCKTAKE_HISTORY_MODAL_SIZE.md`.

- Stocktake submit guard:
  - Added duplicate-submit protection to Warehouse Stocktake and Fridge Stocktake completion flows.
  - Both modules now use an `isStocktakeSubmitting` UI state plus a ref-level runtime lock, so repeated clicks during cloud writes are ignored.
  - Completion buttons disable and show `处理中...` until cloud writes finish or fail.
  - Existing stocktake calculation, inventory write collections, and history formats were preserved.
  - Verified targeted stocktake completion tests, TypeScript check, and production build.
  - Detailed archive: `docs/2026-06-26_STOCKTAKE_SUBMIT_GUARD.md`.

- POS multi-terminal order sync guard:
  - Fixed incremental POS order publishing so pending or weak-network `smartUpdateDocument` results are not marked as cloud-synced.
  - Removed the duplicate POS table-status effect and kept one `reconcileTableStatusFromOrders` path to reduce flicker and competing table writes.
  - Added a kitchen terminal-order guard so completed/cancelled POS orders cannot be regressed by stale kitchen updates.
  - Fixed waiter add-on flow so adding items to a previously paid order recalculates payment status as paid/partial/unpaid.
  - Replaced local-only order-number sequencing with a store-scoped Firestore transaction counter while keeping the visible date-plus-sequence format.
  - Added Firestore rules for `stores/{storeId}/order_counters`.
  - Verified targeted POS regression tests and production build.
  - Deployed Hosting and Firestore rules to Firebase project `restaurant-pos-1b420`.
  - Detailed archive: `docs/2026-06-26_POS_MULTITERMINAL_ORDER_SYNC_GUARD.md`.

- System verification and deploy pass:
  - Fixed stale safety-test expectations after recent readable Chinese/Spanish UI copy changes.
  - Fixed user-visible mojibake in menu image upload errors, POS held-order alerts, and inventory category error alerts.
  - Confirmed remaining alert/confirm/prompt mojibake hits are only intentional test guard strings.
  - Verified `dataSafety.test.ts` passes: 176 tests.
  - Verified full Jest suite passes: 18 suites, 243 tests.
  - Verified production build compiles successfully.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Verified production login page and owner dashboard with Playwright after `admin/admin123` login.
  - Detailed archive: `docs/2026-06-26_SYSTEM_VERIFICATION_DEPLOY.md`.

- Low-risk mojibake cleanup:
  - Cleaned old mojibake comments and console logs from `POS.tsx` and `smartSyncService.ts`.
  - Fixed future POS delivery-fee expense labels and cancellation operator labels that still contained mojibake.
  - Re-scanned both files: 0 mojibake matches and 0 mojibake console messages.
  - Verified `dataSafety.test.ts`: 176 tests passed.
  - Verified full Jest suite: 18 suites, 243 tests passed.
  - Verified production build compiles successfully.
  - Detailed archive: `docs/2026-06-26_LOW_RISK_MOJIBAKE_CLEANUP.md`.

- System source cleanup scan:
  - Scanned production source under `client/src/**/*.ts(x)` excluding regression tests.
  - Removed remaining production `console.log(...)` debug noise while keeping `console.warn` and `console.error`.
  - Replaced remaining POS mojibake comments with stable ASCII comments.
  - Removed debug-only `syncedCount` and `failCount` leftovers from `DataService.ts` and `smartSyncService.ts`.
  - Re-scanned production source with PowerShell and UTF-8 Node readers: 0 mojibake matches and 0 runtime `console.log` statements.
  - Checked direct key business `localStorage.setItem` hits; remaining hits are store-scoped refresh/table caches, not bare global business overwrites.
  - Verified `dataSafety.test.ts`: 176 tests passed.
  - Verified full Jest suite: 18 suites, 243 tests passed.
  - Verified production build compiles successfully with no warnings.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Verified deployed URL returns 200, serves `main.f37e6671.js`, and loads the login page with 0 Playwright page errors.
  - Detailed archive: `docs/2026-06-26_SYSTEM_SOURCE_CLEANUP_SCAN.md`.

- Console Firestore noise fix:
  - Investigated production console reports showing many warnings/errors.
  - Confirmed a fresh browser loaded the current deployed bundle while the user's Chrome log still referenced an older stale bundle.
  - Replaced deprecated Firestore persistence setup with `initializeFirestore` plus `persistentLocalCache` and `persistentMultipleTabManager`.
  - Set Firebase SDK log level to `silent` to avoid expected SDK weak-network console floods.
  - Kept weak-network local fallback and pending sync behavior, but stopped expected fallback paths from logging as warning/error noise.
  - Fixed the bad literal warning template that printed `$collectionName/$docId`.
  - Removed two route-guard development warnings that were harmless but noisy after login redirects.
  - Added Hosting no-cache headers to reduce stale app-shell/bundle issues after deployment.
  - Verified `dataSafety.test.ts`: 176 tests passed.
  - Verified full Jest suite: 18 suites, 243 tests passed.
  - Verified production build compiles successfully as `main.fa1ba0eb.js`.
  - Detailed archive: `docs/2026-06-27_CONSOLE_FIRESTORE_NOISE_FIX.md`.

- Fridge stocktake selected-fridge completion:
  - Changed Fridge Stocktake completion from all-fridges-at-once to selected-fridge-only, matching the practical Warehouse Stocktake workflow.
  - Uncounted checks, discrepancy checks, fridge inventory cloud writes, and history writes now scope to the selected fridge.
  - Count input state now uses `fridgeId:itemId`, preventing the same item in multiple fridges from sharing a count.
  - Compacted the Fridge Stocktake header, action buttons, fridge selector, and search row so the item list has more visible space.
  - Added regression coverage for selected-fridge completion and compact header layout.
  - Verified targeted stocktake tests: 12 passed.
  - Verified `dataSafety.test.ts`: 177 tests passed.
  - Verified full Jest suite: 18 suites, 244 tests passed.
  - Verified production build compiles successfully as `main.1c54fa4b.js`.
  - Detailed archive: `docs/2026-06-27_FRIDGE_STOCKTAKE_SELECTED_FRIDGE_FIX.md`.

- Expense Records search and ranking:
  - Added a keyword search box to Store Manager -> Expense Records.
  - Search applies inside the active date, parent category, and child category filters.
  - Search covers expense text, supplier names, order numbers, and linked purchase item details such as chicken or vegetables.
  - Added current-filter category expense ranking and item/detail expense ranking.
  - Revised the Expense Records layout so date filters stay at the top, the records list appears before rankings, and rankings sit below the list in compact grouped form.
  - Date filtering now supports all, today, specific date, and month, with today as the default.
  - Removed redundant Today/All shortcut buttons from the toolbar because the date-mode selector already covers those modes.
  - Changed Expense Records to scroll vertically inside the app shell and bounded the records table height so the bottom ranking block is visible instead of clipped.
  - Centralized expense-to-purchase-order matching in `client/src/utils/expensePurchaseLink.ts`.
  - Updated Expense Records, Manager Overview expense analytics, and Financial Reports daily details to use the same purchase linkage path.
  - Verified targeted tests: `expenseRecordInsights.test.ts` plus `dataSafety.test.ts`, 182 tests passed.
  - Verified production build compiles successfully as `main.a6b3b907.js`.
  - Verified local and deployed Expense Records page with Playwright after `zeng/123456` login; console errors/warnings: 0.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`.
  - Detailed archive: `docs/2026-06-27_EXPENSE_RECORDS_SEARCH_RANKING.md`.

- Expense Records large-container layout correction:
  - Corrected the follow-up layout after visual review: the list no longer uses a small inner vertical scroll area.
  - Records now expand naturally inside an outer white page container; the outer app page handles vertical scrolling.
  - Kept date filters above the list and rankings below the list.
  - Verified targeted tests: `expenseRecordInsights.test.ts` plus `dataSafety.test.ts`, 182 tests passed.
  - Verified production build compiles successfully as `main.49756f43.js`.
  - Browser verification at 1366x768 showed 12 records with 7 visible rows and no table-inner vertical scroll.
  - Screenshot: `client/output/playwright/expense-records-large-container-dev-1366.png`.

- POS clear-table flicker fix:
  - Investigated the occasional 4-5 repeated table flashes when clearing a dine-in table.
  - Root cause: `pos_tables` realtime snapshots could briefly replay an older table state over the newer local clear-table state.
  - Added `mergeTablesByVersion` so cloud snapshots are merged by `lastModified` and stale table snapshots cannot overwrite newer local state.
  - Kept newer cloud updates from other terminals valid, preserving multi-device sync.
  - Added regression coverage for the stale snapshot guard.
  - Verified targeted stale-snapshot test passes.
  - Verified POS table regression group: 11 tests passed.
  - Verified production build compiles successfully as `main.5e4f1c13.js`.
  - Detailed archive: `docs/2026-06-28_POS_CLEAR_TABLE_FLICKER_FIX.md`.

## 2026-06-30 Commercial rollout baseline
- Created `docs/COMMERCIAL_ROLLOUT_PLAN.md` as the single commercial rollout progress board.
- Completed Step 1, project baseline freeze:
  - Confirmed official project path is `C:\Users\华为\Desktop\Restaurant_POS_Cloud_Clean`.
  - Added generated verification folders to `.gitignore`: `/.playwright-cli/`, `/output/`, and `client/output/`.
  - Archived details in `docs/2026-06-30_COMMERCIAL_BASELINE_FREEZE.md`.
- Completed Step 2, data safety baseline:
  - Documented global and store-scoped Firestore collections.
  - Fixed backup export coverage so every `stores/{storeId}/...` collection in `firestore.rules` is included.
  - Added regression coverage that parses `firestore.rules` and verifies backup coverage.
  - Verified `dataSafety.test.ts`: 191 tests passed.
  - Verified production build: `main.41fbf153.js`.
  - Deployed Hosting only to Firebase project `restaurant-pos-1b420`; live URL returned HTTP 200 with `main.41fbf153.js`.
  - Archived details in `docs/2026-06-30_DATA_SAFETY_BASELINE.md`.
- Next commercial rollout step: POS lifecycle hardening.

## 2026-06-30 POS lifecycle merge hardening
- Extracted POS lifecycle merge/status rules from the large POS component into `client/src/utils/posLifecycle.ts`.
- Added focused regression coverage in `client/src/utils/posLifecycle.test.ts` for:
  - cloud completed overriding stale local paid/served state
  - cloud cancelled overriding stale local confirmed state
  - local completed state resisting older unpaid cloud snapshots
  - pending terminal orders waiting for matching cloud echo
  - table status transitions for unpaid, paid, completed, and cancelled orders
- Updated `dataSafety.test.ts` to guard the new utility location.
- Verified `posLifecycle.test.ts`: 5 tests passed.
- Verified `dataSafety.test.ts`: 191 tests passed.
- Verified production build: `main.a0adf546.js`.
- Deployed Hosting only to Firebase project `restaurant-pos-1b420`; live URL returned HTTP 200 with `main.a0adf546.js`.
- Browser smoke check after `zeng/123456` login opened `/pos`, rendered `Mesas` and `Pedidos`, and reported 0 console/page errors.
- Added repeatable no-mutation POS smoke script: `npm run verify:pos-smoke -- --password <password>`.
- Verified script with `zeng/123456`: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0, bundle `/static/js/main.a0adf546.js`.
- Archive: `docs/2026-06-30_POS_LIFECYCLE_MERGE_HARDENING.md`.
- Remaining POS lifecycle work: controlled E2E order tests should run against staging/emulator instead of production data.

## 2026-06-30 POS lifecycle read-only audit
- Added `client/scripts/auditPosLifecycle.mjs`, a read-only Firestore audit for store-scoped POS orders and tables.
- Added package script: `npm run audit:pos-lifecycle -- --password <password> --hours 96`.
- Added data-safety guard proving the audit script reads `stores/{storeId}/pos_orders` and `stores/{storeId}/pos_tables`, checks known mismatch types, and does not call Firestore write APIs.
- Corrected audit recency logic so completed/cancelled order business checks use `completedAt` or `createdAt`, not `lastModified`; this prevents old orders touched by sync from being misclassified as recent stock-deduction failures.
- Ran production read-only audit against `restaurant-pos-1b420`: Bluefields 1299 orders / 13 tables / 3 active orders, Managua 0 orders / 0 tables, total issues 0.
- Saved latest clean audit JSON to `docs/pos-lifecycle-audit-latest.json`.
- Verified targeted tests: `posLifecycle.test.ts` and `dataSafety.test.ts`, 197 tests passed.
- Verified production build: `main.a0adf546.js`.
- Redeployed Firebase Hosting; live URL returned HTTP 200 with `main.a0adf546.js`.
- Verified POS smoke script after `zeng/123456` login: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.

## 2026-06-30 POS order card visual state guard
- Found a real POS risk in `getStatusColor`: a mojibake inline comment had swallowed the `confirmed` color case, so confirmed unpaid orders could fall back to the default neutral color.
- Added tested utility rules in `client/src/utils/posLifecycle.ts`:
  - unpaid active orders (`confirmed`, `preparing`, `served`) -> red card `#fecaca`
  - paid active orders -> orange card `#fed7aa`
  - terminal orders (`completed`, `cancelled`) -> neutral card `#ffffff`
  - status text remains Spanish: `Pagado`, `Completado`, `Cancelado`, etc.
- Updated POS to call `getPosOrderCardColor` and `getPosOrderStatusText` instead of keeping fragile inline switch logic in the large POS component.
- Restored the split-table recovery lines that were accidentally removed while cleaning the corrupted POS block; data-safety guard confirms `deletedTableIdsRef.current.delete(restoredTable.id)` is present.
- Verified targeted tests: `posLifecycle.test.ts` and `dataSafety.test.ts`, 198 tests passed.
- Verified production build: `main.4031293d.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.4031293d.js`.
- Verified POS smoke script after `zeng/123456` login: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.

## 2026-06-30 POS controlled lifecycle flow coverage
- Added a pure controlled POS dine-in lifecycle flow test in `client/src/utils/posLifecycle.test.ts`.
- Added `getPosLifecycleSnapshot` in `client/src/utils/posLifecycle.ts` to verify order card color/text and table status from the same lifecycle rule set.
- Covered these states without creating production orders:
  - confirmed unpaid order -> table `occupied`, red card, `Confirmado`
  - paid active order -> table `needs_cleaning`, orange card, `Pagado`
  - completed and cleared order -> table `available`, neutral card, `Completado`
  - cancelled order -> table `available`, neutral card, `Cancelado`
- Verified targeted tests: `posLifecycle.test.ts` and `dataSafety.test.ts`, 199 tests passed.
- Verified production build: `main.887a9363.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.887a9363.js`.
- Verified POS smoke script after `zeng/123456` login: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.

## 2026-06-30 Inventory lifecycle audit and negative-stock guard
- Added `client/scripts/auditInventoryLifecycle.mjs`, a read-only Firestore audit for store-scoped inventory collections.
- Added package script: `npm run audit:inventory-lifecycle -- --password <password> --hours 96`.
- Audit reads these store-scoped collections without using Firestore write APIs:
  - `inventory_items`
  - `fridges`
  - `fridge_inventory`
  - `stock_transfer_records`
  - `warehouse_stocktake_history`
  - `fridge_stocktake_history`
  - `purchase_orders`
  - `expenses`
- Production audit result for `restaurant-pos-1b420`:
  - Bluefields: 76 inventory items, 3 fridges, 39 fridge inventory rows, 14 transfer records, 34 warehouse stocktake records, 7 fridge stocktake records, 83 purchase orders.
  - Managua: 0 records.
  - Issues: 7 critical, all `negative_warehouse_stock`.
- Current negative warehouse items found:
  - `TE VASO`: `-51 BOT`
  - `extra salsa`: `-0.30000000000000004 lb`
  - `水饺`: `-60 lb`
  - `Extra9*9 包装盒`: `-8 个`
  - `diferencia`: `-6 lb`
  - `Extra7*7 包装盒`: `-49 个`
  - `Extra8*8 包装盒`: `-76 lb`
- Root cause found in code: POS stock deduction planned fridge deductions first, but any remaining quantity was sent to warehouse without checking that warehouse stock was sufficient.
- Added `client/src/utils/stockDeduction.ts` and `client/src/utils/stockDeduction.test.ts`.
- POS stock deduction now builds a complete plan before writing:
  - fridge stock is consumed first.
  - warehouse covers only the shortage.
  - if fridge plus warehouse cannot cover the sale, the completion flow throws before any stock write, preventing new negative warehouse values.
- Added data-safety guard so AppContext keeps using `buildStockDeductionPlan` before `smartIncrementField('inventory_items', ...)`.
- Saved latest inventory audit JSON to `docs/inventory-lifecycle-audit-latest.json`.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 203 tests passed.
- Verified production build: `main.d9da7682.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.d9da7682.js`.
- Verified POS smoke script after `zeng/123456` login: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.

## 2026-06-30 Purchase stock increment idempotency
- Continued Step 4 inventory lifecycle hardening.
- Added a data-safety guard that requires purchase order stock increments to pass a stable `syncOperationId`.
- Verified the new guard failed before the production code change.
- Updated `client/src/pages/Inventory/PurchaseManagement.tsx` so each purchase line uses `purchase-stock-${order.id}-${orderItem.itemId}-${itemIndex}` when calling `smartIncrementField('inventory_items', ...)`.
- This makes replay of the same purchase order line idempotent and reduces weak-network duplicate stock addition risk.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 203 tests passed.
- Verified production build: `main.244a9c1a.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.244a9c1a.js`.
- Verified POS smoke script after `zeng/123456` login: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Archive: `docs/2026-06-30_PURCHASE_STOCK_INCREMENT_IDEMPOTENCY.md`.

## 2026-06-30 Inventory stock in/out records ledger
- Reused the existing Inventory module stock in/out records tab instead of creating a separate adjustment module.
- Added store-scoped cloud path `stores/{storeId}/inventory_stock_records` to Firestore rules, backup export coverage, and inventory lifecycle audit.
- Updated the Inventory page so the records tab reads `inventory_stock_records` from cloud and stores a scoped local cache only as fallback.
- Warehouse stocktake discrepancies now write audited `adjust` records to `inventory_stock_records`.
- Fridge stocktake discrepancies now write audited `adjust` records to `inventory_stock_records`, including fridge id/name.
- Verified red-green guard: the new data-safety test failed before implementation and passed after.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 204 tests passed.
- Verified production build: `main.1fc7560e.js`.
- Deployed Firebase Hosting and Firestore rules; live URL returned HTTP 200 with `main.1fc7560e.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` stock in/out records tab visible and rendered, console/page errors 0.
- Inventory audit can read `inventory_stock_records`; it still exits with known 7 existing negative warehouse stock records.
- Archive: `docs/2026-06-30_INVENTORY_STOCK_RECORDS_LEDGER.md`.

## 2026-06-30 Purchase stock record ledger
- Continued Step 4 inventory lifecycle hardening.
- Added a data-safety guard requiring purchase order creation to write `source: 'purchase_order'` records into `inventory_stock_records`.
- Verified the new guard failed before implementation and passed after.
- Purchase order item lines now write deterministic `type: 'in'` stock ledger records with purchase order id, order number, supplier, before stock, quantity, after stock, item, unit, and timestamp.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 204 tests passed.
- Verified production build: `main.cf1a78a2.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.cf1a78a2.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` stock in/out records tab visible and rendered, console/page errors 0.
- Archive: `docs/2026-06-30_PURCHASE_STOCK_RECORD_LEDGER.md`.

## 2026-06-30 Fridge transfer stock record ledger
- Continued Step 4 inventory lifecycle hardening.
- Added a data-safety guard requiring fridge transfer transactions to write paired `inventory_stock_records` rows.
- Verified the new guard failed before implementation and passed after.
- `smartTransferFridgeStock` now writes two deterministic stock ledger records inside the same Firestore transaction as the transfer:
  - `${operationId}-warehouse`
  - `${operationId}-fridge`
- Weak-network pending fridge transfers also keep local pending ledger rows and replay through the same cloud transaction when syncing.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 204 tests passed.
- Verified production build: `main.95140e35.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.95140e35.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` stock in/out records tab opened and rendered, console/page errors 0.
- Archive: `docs/2026-06-30_FRIDGE_TRANSFER_STOCK_RECORD_LEDGER.md`.

## 2026-06-30 POS sale stock record ledger
- Continued Step 4 inventory lifecycle hardening.
- Added a data-safety guard requiring POS completion stock deductions to write `source: 'pos_sale'` records into `inventory_stock_records`.
- Verified the new guard failed before implementation and passed after.
- `deductStock` now accepts optional order source metadata, and POS completion passes operation id, order id, order number, order type, and completion timestamp.
- POS sales now write deterministic out-ledger records for both warehouse and fridge deductions:
  - `${operationId}-warehouse-${item.id}`
  - `${operationId}-fridge-${recordId}`
- POS stock increments now pass stable `syncOperationId` values based on the order stock deduction operation id.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 205 tests passed.
- Verified production build: `main.32e8a6a4.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.32e8a6a4.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` stock in/out records tab opened and rendered, console/page errors 0.
- Archive: `docs/2026-06-30_POS_SALE_STOCK_RECORD_LEDGER.md`.

## 2026-06-30 Inventory stock record date normalization
- Added a follow-up data-safety guard for Firestore Timestamp dates in the stock in/out records page.
- Verified the new guard failed before implementation and passed after.
- `Inventory.tsx` now normalizes stock record dates from Firestore Timestamp, Date, string, or millisecond timestamp before sorting and rendering.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 206 tests passed.
- Verified production build: `main.e9cae7ac.js`.
- Deployed Firebase Hosting; live URL returned HTTP 200 with `main.e9cae7ac.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` stock in/out records tab opened, console/page errors 0, and no `Invalid Date` text was present.
- Archive: `docs/2026-06-30_INVENTORY_STOCK_RECORD_DATE_NORMALIZATION.md`.

## 2026-06-30 Inventory item edit stock audit
- Continued Step 4 inventory lifecycle hardening.
- Added a data-safety guard requiring manual edits to an existing inventory item's warehouse stock to write an `inventory_stock_records` adjustment record.
- Verified the new guard failed before implementation and passed after.
- `Inventory.tsx` now creates a signed `adjust` record with before stock, after stock, timestamp, source, and source item id when `currentStock` changes in the item edit modal.
- This records the manual stock adjustment only; it does not apply a second stock mutation and does not auto-correct existing negative values.
- Verified targeted tests: `stockDeduction.test.ts`, `posLifecycle.test.ts`, and `dataSafety.test.ts`, 210 tests passed.
- Verified production build: `main.2a048f92.js`.
- Deployed Firebase Hosting; live URL returned `main.2a048f92.js`.
- Verified POS browser smoke: `/pos` rendered `Mesas` and `Pedidos`, console/page errors 0.
- Verified inventory browser check: `/inventory` opened, negative-stock filter remained visible, console/page errors 0.
- Archive: `docs/2026-06-30_INVENTORY_ITEM_EDIT_STOCK_AUDIT.md`.

## 2026-06-28 POS 订单同步缓存污染修复
- 已修复 POS 本地旧订单缓存可反向发布到云端的问题：现在只有明确待同步订单才会写云端，避免旧设备把历史订单号/旧状态推回去。
- 已修复云端 `completed/cancelled` 终态在 B 端被本地旧状态挡住的问题：除非同一订单正在本机待同步，否则云端终态优先。
- 已修复弱网本地订单号兜底可能从低序号重新开始的问题：现在会参考当天本地最大 `MMDD###` 后续号。
- 已过滤右侧订单列表中的本地空占位订单，避免出现橙色 `0` 订单卡片。
- 已补 Firestore `expense_records` 规则，修复正常网络下因权限导致的“刷新失败/检查网络”提示。
- 验证：POS 同步相关回归测试通过，`npm run build` 通过，已部署 Firebase Hosting + Firestore rules。线上包：`main.505a6ffa.js`。详见 `docs/2026-06-28_POS_ORDER_SYNC_CACHE_GUARD.md`。
