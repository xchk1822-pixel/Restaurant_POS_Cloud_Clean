# 2026-06-10 Progress Notes

## 已完成

- 库存模块：物品管理、菜品管理、仓库盘点、冰箱盘点、供应商管理已改为分店隔离的云端/本地存储模式，并增加手动刷新入口，减少旧本地数据覆盖云端新数据的问题。
- 菜品图片：新增浏览器端压缩，保存缩略图和中图；图片文件上传到 Firebase Storage，菜单记录只保存 URL 和路径。
- 菜品图片跨终端：已启用 Firebase Storage，并部署菜单图片专用规则。A 终端上传后，B 终端菜品管理和 POS 点餐列表可读取云端图片。
- POS 菜品图片展示：菜品图片在点餐卡片中显示，名称和价格覆盖在图片上方，图片使用 contain 显示，避免裁切过多。
- 图片上传稳定性：在线上传失败时不再静默保存为本地成功，避免 A 端本地缓存可见、B 端无图的假同步。离线场景仍保留本地待上传。
- 订单库存扣减：堂食、打包、外卖统一为订单完成时扣减库存；支付不扣减，便于顾客先支付后加菜。
- 打包/外卖订单：补充已取餐/完成入口，完成后触发与堂食一致的库存扣减。

## 当前确认

- Firebase Hosting 已部署：`https://restaurant-pos-1b420.web.app`
- Firebase Storage rules 已部署成功。
- 最新相关提交：`a542c91 Fix menu image cloud upload handling`

## 2026-06-10 继续完善

- 员工管理模块：已改为低频数据模式，不使用实时订阅；删除员工时写入云端删除登记并保留本地 tombstone，避免旧终端或旧路径数据把已删除员工恢复；页面增加手动刷新云端数据和最后同步时间。
- 店长管理模块：数据概览和财务报表改为进入页面时拉取一次云端数据，之后由“刷新云端数据”按钮手动同步；同步后写入本地缓存并显示最后同步时间，不再依赖后台实时订阅。
- 店长管理模块补漏：开支记录、交班对账、历史订单、客户管理也已改为低频手动刷新模式；删除开支/客户、清空交班历史会同步删除云端记录，票据图片、开支类别、客户积分流水会同时保存本地和云端。
- 当前低频模块原则：库存管理、员工管理、店长管理都采用“本地可用 + 云端保存 + 手动刷新 + 同步时间”的方式，减少 Firestore 读取消耗，并降低旧本地数据覆盖云端新数据的风险。
- 老板后台：`/dashboard` 已改为手机优先的全局后台界面；不使用实时订阅，进入页面读取缓存并手动刷新云端数据，显示最后同步时间，按今天/近7天/近30天汇总分店营业额、订单、利润、成本、支付方式和提醒。

## 下一阶段

- 员工管理模块：按低频数据处理，不使用实时订阅；采用进入页面读取一次、保存时写云端、需要时手动刷新云端数据的模式，降低 Firestore 读取消耗。
- 员工删除：改为云端软删除标记 `isDeleted`，读取员工数据时过滤已删除记录，避免旧终端或旧缓存把已删除员工重新恢复。
- 员工管理模块继续检查：员工列表、考勤、借款、工资结算、现金流水的本地/云端保存、刷新恢复、多设备同步和旧数据覆盖问题。
- 原则：先修数据链路和同步，不重做界面，不扩大业务逻辑改动范围。


## 2026-06-10 ????????

- ??????????? + ?????? + ??????????????????????????????????????????????
- ???????????????????????????????????????????????????????
- ???????????????????????????????????????/???????????????????????
- ???????????????????? /settings/data-sync ??????????????????

## 2026-06-10 员工管理继续完善

- 员工档案：删除员工后立即从当前页面列表移除，同时写入云端软删除标记和 employee_deletions 删除登记，避免刷新后旧员工恢复。
- 员工档案：新增和编辑员工统一通过 smartSyncService 写入云端和本地缓存，不再额外写旧 DataService 路径。
- 考勤、借款、工资结算：移除旧 DataService 双写，保存时统一走 smartSyncService，减少同一模块数据在不同本地 key 之间分叉。
- 验证：TypeScript 和生产构建通过；员工模块继续保持“进入页面/手动刷新拉云端，保存动作写云端和本地缓存”的低频模式。

## 2026-06-10 系统设置继续完善

- 汇率设置：重写为本地 `global_exchange_rate` + 云端 `exchange_rate/global` 保存，修复原来保存到 `exchange_rate` 但系统读取 `global_exchange_rate` 的路径不一致问题。
- 汇率设置：增加手动刷新云端数据和最后同步时间；保存后继续触发 `exchangeRateUpdated`，让当前终端 POS/支付等组件立即使用新汇率。
- 旧数据同步：删除未使用的 `DataSyncPage` 文件，并确认 `/settings/data-sync` 路由和菜单入口都已清零。

## 2026-06-10 旧页面和采购支出整理

- 删除未被路由引用的旧客户管理页 `pages/Customers/Customers.tsx`，避免旧页面继续自动写 `pos_customers` 和旧 DataService 路径。
- 删除未被引用的旧分店菜单页 `StoreMenuManager` 及其旧服务 `menuManagementService`，避免后续误接入造成菜品/菜单数据重复链路。
- 采购管理：现金采购生成采购支出时，改为明确写入财务本地缓存，再写云端 `expenses`，避免 `dataManager.addData` 和 `smartAddDocument` 双重隐式写入。

## 2026-06-10 员工管理数据链路补强

- 员工模块云端刷新后，员工、考勤、借款、薪资、现金流水都会写回当前分店本地缓存，避免只刷新页面状态、不落本地导致断网或重新进入后数据不一致。
- 员工借款生成的开支记录改为先明确写入财务本地缓存，再写云端 `expenses`，避免财务报表刷新后员工借款开支丢失。
- 薪资结算生成的开支记录同样改为本地缓存 + 云端 `expenses` 显式保存，保持与采购支出、普通开支一致的数据链路。
## 2026-06-10 权限管理整理

- 权限管理继续保持固定四个门店角色：店长、收银、服务生、厨师；界面不再提供新增角色入口，避免重复角色继续扩散。
- 默认权限清理：移除已删除的 `/settings/data-sync` 相关权限；补齐 `inventory:suppliers` 到权限树和店长默认权限，保持库存模块五个子功能一致。
- 权限树选择逻辑补强：父节点查找改为递归查找，支持多层权限树，并清除本模块生产构建警告。
- 验证：TypeScript 检查通过，生产构建通过；线上 `admin/admin123` 登录后可进入权限管理，四个固定角色显示正常，旧 `data-sync` 权限未出现，浏览器控制台错误数为 0。

## 2026-06-10 数据安全备份入口

- 新增系统设置下的“数据备份”页面，权限为 `settings:backup`，默认仅老板账号拥有。
- 备份导出为只读操作：读取 Firestore 全局集合、各分店业务集合和当前浏览器餐厅本地缓存，下载 JSON 文件，不写入、不恢复、不覆盖任何数据。
- 导出结果会显示总记录数、分店数、读取失败集合和本地缓存键数量；图片文件本身不打包，菜品图片 URL 和 Storage 路径会随菜品记录保留。
- 旧 `/data-recovery` 公开入口已重定向到受权限保护的 `/settings/backup`，避免旧恢复工具被直接打开并写回本地缓存。
- 旧维护/测试公开路由 `/migrate`、`/migrate-users`、`/emergency-fix`、`/firebase-test`、`/offline-test`、`/data-init` 已统一重定向到受权限保护的 `/settings/backup`，避免生产环境误执行迁移、演示数据初始化或紧急建号工具。

## 2026-06-10 登录和用户写入安全

- 登录页默认关闭 localStorage 本地账号兜底登录：Firebase Auth 登录失败后直接失败；只有显式设置 `REACT_APP_ENABLE_LOCAL_LOGIN_FALLBACK=true` 时才允许开发过渡用的本地账号迁移逻辑，避免生产环境本地数据影响真实身份和角色。
- Firestore `users` 集合写入收紧为仅超级管理员可创建、更新、删除；普通已登录用户仍可读取用户资料，但不能自行写入或提升角色。
- 后续需要单独完善“老板创建账号时同步创建 Firebase Auth 用户”，避免只写 Firestore 用户文档导致新账号无法 Firebase 登录。
## 2026-06-10 Branch Account Auth Link

- Owner store/account management now creates new branch accounts through Firebase Auth first, using a secondary Firebase Auth app so the current admin session is not replaced by the newly created user.
- New user documents use the Firebase Auth UID as `users/{uid}` and no longer persist plaintext passwords to Firestore/local user cache.
- Existing user edits are limited to profile fields such as name and role. Username changes are blocked because username maps to the Firebase Auth login email.
- Password reset from the frontend is intentionally disabled with a clear message; it needs a later secure admin endpoint to change Firebase Auth passwords safely.

## 2026-06-10 Supplier Payment Expense Link

- Supplier repayment now writes the finance expense record explicitly to local finance cache and Firestore `expenses`, matching the cash purchase expense path.
- Removed the remaining `dataManager.addData('expenses')` path from supplier repayment to avoid hidden or inconsistent finance writes.

## 2026-06-10 Financial Supplier Debt Logic

- Financial report purchase expense now follows cash-flow logic: cash purchases and supplier repayments count as purchase expense on the payment date; unpaid credit purchases do not count as same-day purchase expense.
- Added a supplier payable/debt summary card that totals remaining supplier debt from purchase orders as `totalAmount - paidAmount`.

## 2026-06-10 Financial Report Print Redesign

- Fixed the blank financial report print output by replacing the broken in-page print selector with a dedicated A4 print window.
- Daily print now includes sales, cash, card, order count, paid purchase expense, operating expense, supplier debt, net profit, shift handover counted cash, and cash difference.
- Daily print includes same-day expense details with category, description, and amount.
- Weekly/monthly/custom print uses date-summary rows only and does not include expense details.
- Financial reports now refresh `handovers` from Firestore together with orders, expenses, and purchase orders, so submitted shift handover cash can be compared in the report.
- Verified locally with Playwright: daily print popup contains the report title, summary, handover comparison, and expense details; weekly print popup contains date summary and no expense-detail section.

## 2026-06-11 Supplier Refresh Authoritative Cloud Pull

- Supplier management manual refresh now forces a Firestore server read for `suppliers` and `purchase_orders`.
- Refresh replaces the local supplier and purchase-order state with the cloud result, then writes the same result to the current store local cache.
- This prevents old B-terminal local supplier data from surviving after a supplier was deleted or changed in the cloud.
- Purchase orders refreshed from cloud are also written into `dataManager` purchases cache so financial supplier-debt calculations use the same refreshed data.
- Verified locally and online with Playwright: `zeng/123456` can open supplier management, click refresh, see sync time, and no page errors occur.

## 2026-06-11 Purchase Debt Versioning

- New purchase orders now carry `lastModified`, improving version-based merge behavior across terminals.
- Credit purchase supplier debt is recalculated from purchase orders instead of incrementing the current local supplier balance.
- This reduces the risk of a stale local supplier balance overwriting the correct cloud debt after multi-terminal use.
- Verified with TypeScript, production build, and local Playwright smoke test on the inventory purchase tab.

## 2026-06-11 Inventory Refresh Authoritative Cloud Pull

- Inventory item/category manual refresh now forces Firestore server reads for `inventory_items` and `inventory_categories`.
- Refresh replaces the current inventory item and category state with cloud results and writes them into current-store local cache.
- This prevents old local item/category data from surviving after another terminal changed or deleted records.
- Verified locally with TypeScript, production build, and Playwright: `zeng/123456` opened inventory, clicked refresh, sync time appeared, local store cache keys existed, and no page errors occurred.

## 2026-06-11 Warehouse And Fridge Refresh Authoritative Cloud Pull

- Added `client/src/utils/stocktakeRefresh.ts` and focused tests so stocktake refreshes normalize cloud data without preserving local-only active records.
- Warehouse stocktake manual refresh now forces Firestore server reads for `inventory_items` and `warehouse_stocktake_history`; active inventory replaces current state and current-store local cache.
- Fridge stocktake manual refresh now forces Firestore server reads for `fridges`, `fridge_inventory`, `inventory_items`, and `fridge_stocktake_history`; active fridge data replaces current state and current-store local cache.
- Stocktake history still uses version merging so historical records are not accidentally discarded during refresh.
- Verified with focused Jest test, TypeScript, production build, local Playwright refresh check, Firebase deploy, and online Playwright refresh check.

## 2026-06-11 Employee Cache Delete Consistency

- Added `client/src/utils/employeeRecords.ts` and tests for filtering active employees with both `isDeleted` flags and `employee_deletions` records.
- Employee module cloud refresh now uses the shared active-employee filter before writing the current-store local employee cache.
- Employee add/edit now explicitly writes the filtered employee list to `dataManager` local cache after updating page state.
- Employee delete now writes the filtered active list to `dataManager` local cache after the cloud soft-delete succeeds, so deleted employees do not reappear from the module cache.
- Verified with focused Jest tests, TypeScript, production build, local Playwright refresh check, Firebase deploy, and online Playwright refresh check.

## 2026-06-11 Shift Handover Cloud Authoritative Refresh

- Added `client/src/utils/handoverRecords.ts` and tests to normalize handover ids and sort newest records first.
- Shift handover manual refresh now treats Firestore as authoritative even when the cloud result is empty, replacing the current page state and current-store `handovers` cache.
- Submitted handover records are normalized and sorted before being saved locally, keeping financial-report comparison data in newest-first order.
- Verified with focused Jest tests, TypeScript, production build, local Playwright refresh check, Firebase deploy, and online Playwright refresh check.

## 2026-06-11 Order History Filtering And Print Cleanup

- Added `client/src/utils/orderHistory.ts` and tests for newest-first sorting, local date filtering, and order type normalization.
- Rebuilt the manager order history page display layer with clean Chinese labels, keeping force-server refresh and current-store local order cache writes.
- Fixed date filters for today, yesterday, last 7 days, last 30 days, and custom ranges using Nicaragua local date keys.
- Fixed the print popup HTML so the A4 order list has valid table headers and readable Chinese instead of mojibake.
- Verified with focused Jest tests, TypeScript, production build, local Playwright page and print checks, Firebase deploy, and online Playwright page and print checks.

## 2026-06-11 Manager Overview Finance Metric Alignment

- Added `client/src/utils/financeMetrics.ts` and tests for purchase-related expense detection and paid-purchase versus operating-expense totals.
- Manager data overview now calculates purchase expense from paid purchase/supplier repayment expense records, matching the financial report cash-flow rule.
- Credit purchase order totals no longer reduce same-day dashboard profit until payment is recorded as an expense.
- Operating expense category analysis now excludes purchase-related expenses so purchase payments and daily operating expenses are not mixed.
- Verified with focused Jest tests, TypeScript, production build, local Playwright overview/report refresh checks, Firebase deploy, and online Playwright overview/report refresh checks.

## 2026-06-11 Customer Points Scoped Cache

- Added `client/src/utils/customerPoints.ts` and focused tests for branch-scoped points transaction local cache behavior.
- Customer management no longer falls back from `store_<storeId>_points_transactions` to the old global `points_transactions` key when a branch user is logged in.
- Saving or refreshing points transactions for a branch user writes only the branch-scoped cache and removes the stale global points transaction cache.
- This prevents old/global points transactions from leaking into a branch after refresh or page reload while preserving global cache behavior for owner accounts without a store id.
- Verified with focused Jest tests, TypeScript, production build, and local Playwright: `zeng/123456` opened customer management, clicked refresh, and local storage contained only `store_store_1776725610354_points_transactions`.

## 2026-06-11 Owner Dashboard Store Dedupe

- Added `client/src/utils/ownerDashboardData.ts` and focused tests for owner dashboard record deduplication.
- Owner dashboard now deduplicates branch records by `id` when loading cached data and after cloud refresh, keeping the newest version and ignoring deleted records.
- This prevents old duplicated store documents with the same logical id from rendering duplicate branch cards or double-counting branch summaries.
- Verified locally with Playwright at desktop and mobile widths: `admin/admin123` opens the owner dashboard, branch cards show `Bluefields` and `Managua` once each, and clicking a card expands details without leaving `/dashboard`.

## 2026-06-11 Owner Dashboard Cash-Flow Cost Metrics

- Owner dashboard purchase cost now follows the same cash-flow rule as financial reports: only paid purchase expenses and supplier repayments count as purchase expense.
- Unpaid credit purchases are no longer subtracted from owner dashboard profit until payment is recorded.
- Added supplier debt calculation from purchase-order remaining balances and displayed it in the owner cost summary.
- Verified with focused tests, TypeScript, production build, and local mobile Playwright: owner dashboard shows `已付采购支出`, `日常开支`, and `供应商欠款` while branch cards remain deduplicated.

## 2026-06-11 Legacy Auto Migration Guard

- Removed the old `dataService.migrateGlobalDataToStore()` call from `MainLayout` so login no longer triggers legacy global-to-store migration code.
- Added a production data-safety test to prevent the main layout from reintroducing the legacy migration call.
- This reduces the risk that old global localStorage data could be copied back into a branch cache after future edits.
- Verified with focused test, TypeScript, production build, and local Playwright: `zeng/123456` and `admin/admin123` log in normally and no legacy migration logs appear.

## 2026-06-11 Legacy Migration And Test Tool Removal

- Removed unused legacy migration/test/recovery pages and services: old data migration component, localStorage migration page, demo data init page/service, emergency fix page, Firebase/offline test pages, data recovery page, user migration page/service, and old Firestore migration service.
- Cleaned the stale commented `DataMigration` block from `App.tsx`.
- Extended data-safety tests so production `App.tsx` cannot reintroduce imports/usages of those legacy tools.
- Existing old URLs still redirect to the protected backup page or through permission checks, but the dangerous tool implementations are no longer present in the bundle.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright: admin old test URLs land on `/settings/backup`, store manager old test URL lands on `/manager`, and no legacy tool text is visible.

## 2026-06-11 POS Broad Local Write Removal

- Replaced POS customer creation `dataManager.addData('customers')` with explicit local customer cache save plus `smartSetDocument('customers', id, customer)`.
- Replaced outsourced-delivery fee `dataManager.addData('expenses')` with explicit local expense cache save plus `smartSetDocument('expenses', id, expense)`.
- Added a data-safety test that prevents POS from reintroducing broad `dataManager.addData(...)` writes, because that helper can sync the whole local collection and revive stale local records.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright POS smoke test using `zeng/123456`; no real customer or expense test record was created.

## 2026-06-11 Firestore Upsert For Smart Updates

- Changed `smartUpdateDocument` to write with `setDoc(..., { merge: true })` instead of `getDoc` followed by `updateDoc`.
- This prevents POS table sync from failing with `No document to update` when a local table exists but the cloud document is missing or was deleted concurrently.
- Extended the data-safety test to lock the upsert behavior.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright POS smoke test; the previous `No document to update` console error no longer appears.

## 2026-06-11 Smart Sync Legacy Bulk Writer Removal

- Removed unused `manualSyncToFirestore`, `smartBatchAddDocuments`, and `migrateOldData` exports from `smartSyncService`.
- Kept the normal offline pending-change queue, but removed old bulk localStorage-to-Firestore writers that could overwrite cloud data if reintroduced through a UI.
- Extended the data-safety test to prevent those legacy bulk writers from returning.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright smoke checks for POS and owner dashboard.

## 2026-06-11 AppContext Legacy Auto-Save Removal

- Removed automatic `dataService.saveData(...)` writes from `AppContext` for menu items, suppliers, purchase orders, fridges, and fridge inventory.
- Kept only local-only `dataManager.saveData(..., { syncFirestore: false })` mirrors where screens still need cached menu or purchase data, and guarded them from empty-array cache overwrites.
- Added a production data-safety test so shared module state cannot reintroduce legacy `DataService` auto-save calls.
- This reduces the risk that stale local branch cache can overwrite newer cloud/module data after refresh, login, or future code changes.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright smoke checks for POS, menu management, supplier management, and financial reports.

## 2026-06-11 Expense Record Single-Document Writes

- Removed legacy `DataService` auto-save for expense categories from the expense records page.
- Changed manual expense creation and receipt upload to update the local cache locally and write only the affected expense document to Firestore.
- Added a data-safety test so expense records cannot reintroduce broad `dataManager.saveData('expenses', ...)` cloud writes.
- This prevents an old local expense cache from replaying a full collection back to Firestore when adding one new expense or uploading one receipt.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright on the manager expense records page.

## 2026-06-11 POS Order Cache Local-Only Save

- Changed the POS order mirror `dataManager.saveData('orders', uniqueOrders)` to `syncFirestore: false`.
- POS still publishes changed orders through its explicit `smartUpdateDocument('pos_orders', order.id, ...)` path, but the local cache mirror no longer replays the whole order collection to Firestore.
- Added a data-safety test to prevent broad POS order collection writes from being reintroduced.
- This reduces the risk that stale completed/paid/order-state cache can overwrite newer cloud order state during multi-device use.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright POS smoke check.

## 2026-06-11 Waiter Order Direct Publish

- Service waiter ordering now publishes new orders directly to `pos_orders` with `smartUpdateDocument` instead of relying on a POS screen to relay AppContext state.
- Adding dishes to an existing waiter order now publishes the updated order document with recalculated total, `updatedAt`, and `lastModified`.
- New waiter dine-in orders now also set the table `currentOrderId` when marking the table occupied, keeping POS and waiter table state aligned.
- Added a data-safety test to ensure waiter orders continue to publish directly to shared POS orders.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright waiter smoke check.

## 2026-06-11 Waiter Table Store-Scoped Cache

- Changed waiter table initialization from global `pos_tables` localStorage to `dataService.getStoreKey('pos_tables')`.
- Added a data-safety test to prevent the waiter screen from reading unscoped table cache again.
- This prevents old/global table layouts from leaking into the waiter terminal after login and keeps waiter/POS table data branch-scoped.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright: the waiter page used `store_1776725610354_pos_tables` and did not create/read global `pos_tables`.

## 2026-06-11 Kitchen Status Shared Order Sync

- Kitchen item status now persists to the shared POS order document using item-level `kitchenStatus`.
- Kitchen "出餐完成" now writes the order back as `served` with `servedAt`, so it disappears from kitchen while remaining available for POS payment/completion flow.
- Kitchen filters out `served`, `completed`, and `cancelled` orders instead of only hiding completed/cancelled records.
- Added a data-safety test to keep kitchen status changes wired to `pos_orders`.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright kitchen smoke check.

## 2026-06-11 Permissions And Backup Data Safety

- Confirmed permissions management is constrained to the four store roles: store manager, cashier, waiter, and chef. Cloud roles are normalized into those canonical roles instead of creating duplicates.
- Confirmed the data backup page remains read-only export/download only.
- Removed legacy `DataService` public entry points that could restore from Firestore or replay local cache collections back to Firestore in bulk.
- `DataService.saveData(...)` now only maintains scoped local cache; cloud writes must remain explicit single-document business writes.
- Added data-safety tests to prevent reintroducing role duplication, backup restore/sync UI actions, or legacy bulk overwrite sync methods.

## 2026-06-11 Inventory Category Cloud-Authoritative Refresh

- Changed inventory category initial cloud load to use server reads and replace local categories when cloud data exists.
- Removed the stale-local merge path that could keep deleted or old categories visible after opening the inventory module.
- Manual inventory refresh already uses server reads for items and categories, then updates the scoped local cache and sync timestamp.
- Added a data-safety test to prevent inventory category cloud loads from merging stale local categories again.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright on the inventory page with `zeng/123456`.

## 2026-06-11 Warehouse And Fridge Stocktake Scoped History

- Confirmed warehouse and fridge stocktake refreshes use server reads and replace active inventory/fridge state with normalized cloud data.
- Changed warehouse stocktake history local cache to `dataService.getStoreKey('warehouse_stocktake_history')`.
- Changed fridge stocktake history local cache to `dataService.getStoreKey('fridge_stocktake_history')`.
- This prevents one branch's local stocktake history from appearing in another branch on the same browser while preserving cloud history reads and single-document history writes.
- Added data-safety tests to lock cloud-authoritative active stock refresh and store-scoped stocktake history caches.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright on warehouse/fridge stocktake pages with `zeng/123456`.

## 2026-06-11 Employee Deletion Tombstone Cache

- Confirmed employee refresh reads employees and `employee_deletions` from Firestore server, then filters deleted employees out of the active list.
- Added local scoped caching for `employee_deletions` during employee module refresh.
- Employee delete remains a single-document soft delete on `employees` plus a single-document tombstone write to `employee_deletions`; local employee cache is saved without Firestore replay.
- Added a data-safety test to ensure employee deletion tombstones are fetched, cached, and used with local-only employee cache saves.
- Verified with focused data-safety test, TypeScript, production build, and local Playwright on the employee management page with `zeng/123456`.

## 2026-06-11 Manager Scoped Cache Hardening

- Changed expense category local cache from global `expense_categories` to `dataService.getStoreKey('expense_categories')`.
- Expense category cloud refresh, add, and delete still use the same store-scoped Firestore collection through `smartGetDocuments`, `smartSetDocument`, and `smartDeleteDocument`, but no longer reuse stale global localStorage.
- Changed shift handover draft cash inputs from global `current_inputs` to `dataService.getStoreKey('current_inputs')`.
- Changed shift handover initial history read to `dataManager.getData('handovers')`, so it uses the same store-scoped `rest_v6_final` cache as handover saves.
- Removed direct global `rest_v6_final` removal from the handover clear path; clearing now goes through `dataManager.saveData('handovers', [], { syncFirestore: false })` plus explicit cloud deletes.
- Added data-safety tests to prevent unscoped manager expense category, handover draft, and handover history cache reads/writes from returning.
- Verified with focused data-safety test, TypeScript, production build, local Playwright on manager expense records and shift handover, Firebase Hosting deploy, and online Playwright verification at `https://restaurant-pos-1b420.web.app`.

## 2026-06-11 Owner Dashboard Refresh Wording

- Replaced remaining owner dashboard "manual sync" UI wording with "manual refresh" wording.
- Loading state now shows `刷新中...` instead of `同步中...`, keeping the owner dashboard aligned with the low-frequency manual-refresh design.
- Added a data-safety test to prevent old `数据同步`, `手动同步`, `同步中`, or `syncToFirestore` wording/entry points from returning to the owner dashboard.
- Verified with focused data-safety test, TypeScript, production build, local mobile Playwright using `admin/admin123`, Firebase Hosting deploy, and online mobile Playwright at `https://restaurant-pos-1b420.web.app/dashboard`.

## 2026-06-11 Store Management Single-Document Writes

- Changed store/user local persistence helpers so they only update the local cache and no longer replay every visible store or user document back to Firestore.
- Store creation now writes only the new store document and any newly created user documents, then updates local cache.
- User create/edit now writes only the affected user document, then updates local cache.
- Store edit now checks for visible duplicate store codes and removes hidden duplicate Firestore store documents that match the previous or new store code while preserving the edited store document.
- This targets the old duplicate-store symptom where an older hidden store document could reappear after editing a legacy branch.
- Added a data-safety test to prevent broad store/user collection rewrites and to keep duplicate-store cleanup on the edit path.
- Verified with focused data-safety test, TypeScript, production build, local Playwright on `/settings/stores`, Firebase Hosting deploy, and online Playwright at `https://restaurant-pos-1b420.web.app/settings/stores`.
